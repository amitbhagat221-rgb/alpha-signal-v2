# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Data (local)
data/
*.db
*.sqlite
*.sqlite3
*.csv
*.json
*.pkl
*.parquet

# Output (local)
output/*.html
output/*.json
output/dossiers_*.json
output/email_*.html
output/*.log
output/promotion_gate.txt   # on-demand promotion_gate.py dump, regenerable

# Notebooks (checkpoints)
.ipynb_checkpoints/
*.ipynb_checkpoints

# QA artifacts (Playwright screenshots at repo root)
news-*.png
news_*.png

# Local-only git hooks copy (live in .git/hooks/, not versioned by git anyway,
# but listed here as a reminder that pre-push lives outside source control)
# scripts/hooks/pre-push  ← uncomment if moved into tracked dir later

# Credentials
.env
*.env
credentials.json
config.local.py

# Logs
*.log
logs/

# Testing
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store
Thumbs.db

# Temporary
*.tmp
temp/
tmp/

# Shell scripts
*.sh

# Playwright MCP verification artifacts (per-session DOM snapshots + screenshots)
.playwright-mcp/
flow-*.png

# graphify knowledge-graph artifacts (regenerated by git post-commit hook)
graphify-out/
backups/

# Downloaded news stock photos (regenerate via `python -m sources.news_images`)
cockpit/static/news_img/

# HANDOFF
Updated: 2026-05-24 | Branch: master (0 unpushed before commit, 1 after) | HEAD: `9b48a20` fix(observability): plug ANO=#1 SMALL data-blank-pick hole

## Left off
A continuation of yesterday's NaN-rate audit turned into 28 numbered drive-by entries — closing the analyst-consensus provenance leak (59.3% of universe), shipping per-factor backtest cadence + Newey-West variance correction (see new [ADR 0022](docs/decisions/0022-per-factor-backtest-cadence-newey-west.md)), and uncovering 3 previously-hidden KEEP-verdict signals (bulk_deal SMALL t=2.56, delivery_anomaly_z SMALL t=4.11, sentiment_7d LARGE preliminary t=-3.88). Net: pick gate now 3-dimensional, dossier validator catches soft hallucination, factor screen split into Data Health vs Validation, and historical depth restored for bulk_deals / short_selling / fii_dii_fno_positioning via new [sources/historical_backfill.py](sources/historical_backfill.py).

## Pick up here
1. **Verify tomorrow's 03:30 UTC cron runs cleanly** — new pipeline steps added today (`fetch_regulatory`, `classify_regulatory`, `fetch_broker_recos`) in [config.py:248-266](config.py#L248). Watch `output/pipeline.log`; the broker_recos step is weekly and should populate `broker_recommendations` for ~2,336 slug-mapped stocks (~2hr at NSE-friendly pace).
2. **Fix `ANALYST_CONSENSUS_GROWTH_WITHOUT_ANALYSTS` at source** — 1,447/2,440 stocks (59.3%) have growth pcts but NULL `total_analysts`/`price_target`. Either gate `analyst_consensus.eps_growth_pct` write on PT/count presence ([sources/tickertape_analyst.py](sources/tickertape_analyst.py)), or update [signals/consensus.py](signals/consensus.py) to require an analyst-sourced field before emitting consensus_signal. See checklist Next-3 #2.
3. **Extend `historical_backfill` to FII/DII Cash Segment** — different SEBI source than the F&O positioning we backfilled today. Add `backfill_fii_cash()` function paralleling the F&O one in [sources/historical_backfill.py](sources/historical_backfill.py); endpoint is `https://www.nseindia.com/api/historical/foreignActivity`. ~1hr code + ~30min backfill.

## Watch out
- **`pit_ic_by_tier_v2` now has dual rows per signal (monthly + weekly+NW).** PK is (signal, cap_tier, source) and the new weekly results have source `v2_recompute:weekly+NW<lag>`. When reading the table, filter by source or you'll see two t-stats per cell. Cockpit Factor Library was updated to use only the monthly row.
- **FACTOR_LIBRARY iteration is broken** — `for s in db.FACTOR_LIBRARY` raises `TypeError: string indices must be integers, not 'str'`. The 20-entry list has a malformed entry. Caught when I ran a diagnostic; not blocking today's work because no production code iterates FACTOR_LIBRARY directly. Fix before adding any new factor to the library.
- **v1 archive import is non-idempotent for insider_archive** — re-running [sources/v1_archive_import.py --source insider](sources/v1_archive_import.py) will re-attempt 90K dupes (INSERT OR IGNORE eats them quickly but writes 87K log rows). Only re-run if v1 has fresh days. The sentiment side is safer (PK collisions are dropped silently at the (sid, snapshot_date) level).
- **Weekly cadence covers only 6 of 9 weekly-tagged signals** (no PIT helper for `fii_dii_cash_net` + `fii_dii_fno_positioning` — they're market-level, not per-stock). Cadence registry tags them weekly but reconstruct_pit silently skips them. Sector-portfolio test framework is still needed (deferred — own session).
- **Most recent 3 weekly Fridays (2026-05-08 / 15 / 22) have NULL `fwd_return_20d`** — by definition: we don't have 20 trading days of future prices yet. The IC compute correctly excludes those dates. Don't try to "fix" them.
- **xlrd is now in v1's venv** — `~/alpha-signal/venv/bin/pip install xlrd` was required for `nselib.derivatives.fii_derivatives_statistics`. If v1's venv gets rebuilt, re-install.

## Active plan
[docs/plans/0002-100-factors-and-model.md](docs/plans/0002-100-factors-and-model.md) — Track 3 §3.1b (NSE F&O OI ingest) still the named active phase, but two sessions in a row have been P0 audit/observability drive-bys. Today's work directly advanced §3.2 (insider + sentiment now PIT-ready, 2 new factors past the t-stat threshold via cadence-aware framework) and §3.3a (IC stability weighting — cadence-aware backtest is a prerequisite). Resume 3.1b only after the source-side analyst-consensus leak (Next-3 #2) is closed.

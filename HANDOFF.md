# HANDOFF
Updated: 2026-05-31 | Branch: master (0 unpushed) | HEAD: `3be4e9a` refactor(cockpit): extract _shared + mf.py + split command_centre
_Two sessions merged today: Plan 0007 remediation (`22f1dcf`) + a parallel cockpit mechanical-cleanup refactor (`3be4e9a`)._

## Left off
Plan 0007 was theoretically closed at Phase 8 but Day-0 reality was 1,689 picks all labeled PRELIMINARY-100, zero `trust_verdicts`, cockpit barely changed — "feels like a giant waste of time." Built [tools/trust_backfill.py](tools/trust_backfill.py) to exercise Gates 2/3/4/5/7 against existing rows, which surfaced 4 root-cause bugs + 46 wrong-entity slug mappings (Oil India→IndianOil, Narayana→NHPC, Apollo Micro→Apollo Hospitals, …); all fixed at source. New `/system` Trust Pipeline card on port 3001 now shows real distribution (149 TRUSTED / 1,440 PRELIMINARY / 100 REVIEW) backed by 10,705 verdicts.

In parallel (commit `3be4e9a`), a cockpit code-cleanup refactor landed — **zero behavior change**, verified (old==new JSON coercion on identical data; function-level before/after diff identical modulo data drift; pit_replay PASS; HTTP 200 on all affected routes). New [cockpit/_shared.py](cockpit/_shared.py) (cache decorators + canonical `safe_json_records`) + [cockpit/mf.py](cockpit/mf.py) (9 MF functions lifted out of the stock-detail layer, re-exported from `cockpit.api`); `cockpit/api.py` −439 LOC; `get_command_centre` split into `_cc_plans_and_adrs` + `_cc_factor_library`.

## Pick up here
1. **corporate_actions backfill** — Gate 3 flagged 9 stock_close discontinuities (LIC 802→411 = 1:2 split 2026-05-29 verified; LEM 11.5×; PRAM 2.6×). All are real corp actions our `corporate_actions` table doesn't carry. Without it Gate 3 will keep adding 5-15 noise verdicts per ex-date. Scrape NSE corp-actions feed daily into [sources/](sources/) + dedup vs existing rows in [validators/temporal_continuity.py:_has_recent_corp_action](validators/temporal_continuity.py).
2. **3 stubborn auditor slugs + 4 dark SIDs** — 30-min manual triage. Auditor still flags `DPSC`/`BRIGT`/`CUBEI` (autosuggest can't recover). Round-2 fetch found no slug for `APPA`/`DED`/`MER`/`PUNI`. Manual map via `UPDATE stocks SET mc_slug=? WHERE sid=?` then `python -m sources.moneycontrol_recos --ticker <T>`.
3. **Plan 0006 Phase D — LLM-narrated sector dossiers** (carried from yesterday) — [docs/plans/0006-sector-dossiers.md](docs/plans/0006-sector-dossiers.md) Phase D. Mirror [output/dossier.py:_build_uhs_block()](output/dossier.py) for sector hygiene. 11 LLM calls/night ~₹3-5.

## Watch out
- [tools/trust_backfill.py](tools/trust_backfill.py) is one-shot manual, NOT in PIPELINE_STEPS. Producer-side gates fire forward at fetch time, but for a clean baseline before a release re-run the backfill explicitly + clear stale verdicts: `sqlite3 data/alpha_signal.db "DELETE FROM trust_verdicts WHERE snapshot_date=date('now')"`.
- `cockpit_ops.api.get_health_overview` has a 5-min `_persisted_cache` — Trust panel won't refresh until `rm data/.cockpit_cache/get_health_overview.pkl && sudo systemctl restart alpha-cockpit-ops.service`. The `?refresh=1` flag does NOT clear it (only refreshes `data_health_scores`).
- [tools/data_sanity.py:_mc_slug_name_mismatch_check](tools/data_sanity.py) — `ticker_in` shortcut dropped entirely. Name-substring + ratio≥0.55 is the whole rule. If a legit slug gets flagged, widen the ratio threshold; do NOT re-add ticker-based shortcuts — they're what missed 46 wrong-entity bugs over months.
- [scoring/health_score.py:rollup_pick_uhs](scoring/health_score.py) was a Phase 1 stub reading only 3 of 5 dims with `phase_3_pending`/`phase_4_pending` reasons that nobody updated after Phases 3+4 shipped. Now reads all 5. If a future phase adds a 6th dim, both `compute_uhs` and `_wmean` calls must update — easy to forget.
- 46 retroactively-quarantined broker_recs are tagged `_q_failed_gate='gate_1_identity_retroactive'` (vs forward Gate-1 quarantines which use `gate_1_identity`). Distinguishable in `broker_recommendations_quarantine` queries.
- **Cockpit cache decorators + MF functions moved** (`3be4e9a`). `_ttl_cache`/`_persisted_cache`/`safe_json_records` now live in [cockpit/_shared.py](cockpit/_shared.py) (import from there, not `cockpit.api`). The 9 `get_mf_*` functions live in [cockpit/mf.py](cockpit/mf.py) but are re-exported from `cockpit.api`, so `api.get_mf_*` still resolves — edit them in `mf.py`. Note `cockpit_ops/api.py`'s half of this refactor (the `command_centre` split + coercion dedup) was bundled into the *earlier* `22f1dcf` by a shared-tree `git add`; the two commits together are one logical change.

## Active plan
[Plan 0006 — Sector dossiers](docs/plans/0006-sector-dossiers.md) Phase D (LLM narration). Plan 0007 closed end-to-end after today's backfill + remediation pass (commit `22f1dcf`).

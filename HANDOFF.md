# HANDOFF
Updated: 2026-05-24 (session #3 of day) | Branch: master (0 unpushed before commit, 1 after) | HEAD: `f086361` feat(backtest): per-factor cadence + Newey-West

## Left off
Cleared both CRITICAL items from the morning health report and shipped a redesigned **Health Center** in cockpit (`/system`, renamed in nav from "System" → "Health Center"). The two fixes: (1) widened `tickertape_shareholding._normalise` clamp to 0.05pp + drop-the-column-not-the-row for genuine out-of-range values, and fixed `health_report.py` streak query so historical-but-recovered failures stop firing CRITICAL; (2) gated `signals/consensus.py` SELECT on `(total_analysts IS NOT NULL OR price_target IS NOT NULL)` so Tickertape-only forecast rows never fire consensus_signal — reframed the sanity check `ANALYST_CONSENSUS_GROWTH_WITHOUT_ANALYSTS` into a forward-looking gate validator `CONSENSUS_SIGNAL_WITHOUT_ANALYST_ATTRIBUTION` (provably 0 when the consumer-side gate holds). Health Center: new Overview tab with verdict banner + 4 pillar tiles (Data/Factors/Pipeline/Dossiers) + **Live Issues Inbox** that unifies findings from `tools.health_report` + `tools.data_sanity` + `cockpit_endpoint_audit` + dossier validator into one filterable+searchable feed (severity / source / category radios + dropdowns + text search). All 4 pre-existing tabs (Data/Factors/Pipeline/Inventory) got filter toolbars and the Pipeline tab gained a "Currently broken" panel for un-recovered streaks. After fix: `python -m tools.health_report` reports `⚠ 12 warn` (no CRITICAL); cockpit's Overview shows the same shape (1 CRITICAL is a real cockpit-endpoint audit finding — `insider_timeline` returns empty for 53% of stocks).

## Pick up here
1. **Fix the `insider_timeline` cockpit endpoint** — the one remaining CRITICAL in the new inbox. Audit log says `48/90 (53.3%) sids return empty · sample: ADEL`. Probably a sid/ticker mismatch in [cockpit/api.py](cockpit/api.py) `get_insider_timeline` — check the SELECT path against `insider_trades` (NSE PIT bulk harvester writes `sid`, but the cockpit may be joining by something else for the 53% that fail). 30-60 min.
2. **Drive the 12 WARNs down** — most are expected refresh-cadence noise (annual/quarterly tables STALE at ~54d; refreshed quarterly). The real ones: `regulatory_events` OUTDATED 44d (harvester ran today — confirm classifier ran cleanly), `EXTREME_GROWTH_PCT_IN_TOP_PICKS` (15/300 top picks have >300% growth pcts; already clipped in dossier but warn-level reminder).
3. **Resume Next-3 #2 from the previous handoff** — Extend `historical_backfill` to FII/DII Cash Segment (different SEBI source than the F&O we backfilled yesterday). Endpoint: `https://www.nseindia.com/api/historical/foreignActivity`. ~1hr code + ~30min pull.

## Watch out
- **Cockpit's `system.html` is now Alpine-heavy** — every panel below the Overview uses `x-data` blocks for filter state. Adding new content requires keeping the radio-group + filter pattern consistent (see the Factors tab as the canonical example). The `tab_bar` macro hash-syncs only valid keys; if you add a tab, the URL `#hash` will auto-route.
- **`get_health_overview()` (new) calls `data_sanity.run()` synchronously** — first hit takes ~1.5s. If we add more sanity checks, consider a TTL cache (the existing `_ttl_cache` decorator pattern at the top of api.py).
- **`signals.consensus` gate is consumer-side**, not source-side — `analyst_consensus` rows STILL carry growth pcts without analyst attribution (that's Tickertape forecast feed, which is real data, just not "analyst consensus"). If you write a new consumer of `eps_growth_pct`, replicate the gate (`(total_analysts IS NOT NULL OR price_target IS NOT NULL)`) or your check will fire false positives.
- **`tickertape_shareholding._normalise` now drops bad column values instead of failing the row** — out-of-range values go to `_OUT_OF_RANGE_LOG`, printed at end of run. If you see "Out-of-range values dropped: N" with N>50 daily, investigate — Tickertape may have shipped a real schema change.
- **Health Center adds a screenshot trail** — `health-*.png` files in repo root are from this session's verification. Delete or .gitignore (currently untracked).

## Active plan
[docs/plans/0002-100-factors-and-model.md](docs/plans/0002-100-factors-and-model.md) — Track 3 §3.1b (NSE F&O OI ingest) still the named active phase. Today's work was P0 observability infrastructure (Health Center) — pulls the lever on the user's stated need: "My window through all this is cockpit so need to be very good". Resume 3.1b only after the `insider_timeline` endpoint is fixed and we've validated the new inbox catches the next bug it'll catch before email does.

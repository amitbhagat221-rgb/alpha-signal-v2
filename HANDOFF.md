# HANDOFF
Updated: 2026-05-28 | Branch: master (0 unpushed, working tree dirty) | HEAD: `6409068` feat(plan-0005): Phase E PIT replay + MICRO 4th tier + cockpit 140× cold-restart

## Left off
End-to-end session: MF universe-browser switched to **investable-only default** (8,492 vs 14,364 schemes; "Show all" toggle for the rest) with ETMoney matcher rewritten as token-set subset (recovered SBI Contra / HDFC ELSS / BoI ELSS, +934 mapped) → incremental scrape v2 finished cleanly (3,438 → **3,959 schemes with holdings**, 0 errors); separately wired **`pt_upside` (t=7-9) + `eps_growth` (t=3-5)** into [scoring/screener.py](scoring/screener.py) + added two backtest-derived weight schemes — **SIGNAL_WEIGHTS_RETURN** (∝ |t|) and **SIGNAL_WEIGHTS_SHARPE** (∝ ICIR) — in [config.py:79-129](config.py#L79-L129), plus new [/model/variants](cockpit/templates/model_variants.html) cockpit page showing all 3 schemes side-by-side with divergent-pick stars; also hotfixed broken `/model` (HTTP 500 since the Stage 2 cockpit_ops split — `get_model_overview` is re-exported now from end of [cockpit/api.py](cockpit/api.py#L2659)) and the Plus Jakarta Sans → **Inter** typography refresh (tabular-nums, lifted muted colors from #555570 → #8e90ac for WCAG AA pass).

## Pick up here
1. **`sudo systemctl restart alpha-cockpit`** — Python changes won't load otherwise. Then hit `/model`, `/model/variants`, `/mutual-funds?show_all=0` — all three are blocked behind the restart. Browser hard-refresh once to pick up the new Inter font + colors.
2. **Wire 4-7 more bench factors** into [scoring/screener._load_signals()](scoring/screener.py) — `pledge_quality` (SMALL t=5.9, already in `promoter_signals.pledge_quality` col), `delivery_anomaly_z` (SMALL t=4.76, in `smart_money_scores`), `interest_coverage` MID t=-3.69, `ccc` MID t=2.79, `roic` SMALL t=-2.71, `fcf_margin` SMALL t=-2.65, `nwc_to_revenue` MID t=2.74. Each is 1-2 lines in `_load_signals()` + an entry in `SIGNAL_COLS`. Re-run `python -m tools.optimize_weights` after — it'll auto-rebuild the dicts and the WIRED_KEYS coverage % drops from 86% unwired to <20% unwired in LARGE/MID.
3. **Decide if a variant becomes the new production** — `SIGNAL_WEIGHTS_RETURN` and `SIGNAL_WEIGHTS_SHARPE` currently print-only / cockpit-only (no `daily_picks` write). To promote one to live: either replace `SIGNAL_WEIGHTS` in [config.py:51](config.py#L51), OR add a `variant` column to `daily_picks` (PK becomes (sid, pick_date, variant)) and run all three nightly. Conservative path is replace-once after a 30-day side-by-side track.

## Watch out
- **`cockpit/api.py` ends with a back-import** from `cockpit_ops.api` ([cockpit/api.py:2659](cockpit/api.py#L2659)): `get_model_overview` and `get_backtest_roster`. Don't reorder — the back-import has to sit AFTER every locally-defined symbol so `cockpit_ops`'s `from cockpit.api import _persisted_cache, _ttl_cache` resolves cleanly during the round-trip. If you move those functions back to cockpit/api.py, remove the import.
- **Variant runs go through the screener but skip `daily_picks`** ([scoring/screener.py:380](scoring/screener.py#L380)) — `compute(variant='return')` prints + returns count, doesn't write. The per-variant cockpit cache is `data/.cockpit_cache/model_variants__top_per_tier=N.pkl` (30-min TTL); if you change weights in config.py or the optimizer, `rm` it before next visit or the page serves stale picks.
- **`pt_upside` is now clipped to ±50/+150% on output** by [signals/consensus.py:178](signals/consensus.py#L178) — backstop for yfinance broken-PT outliers (CCAVENUE had +33,522%, ABCOTS +15,894%). 18 existing rows in `consensus_signals` got `UPDATE`d to the clipped values during this session; future writes inherit it. The Spearman IC backtest (rank-based) is unaffected so the t=9 result still holds.
- **Cockpit cache had 13 stale pickles** before the session ended — cleared. After any `consensus_signals` edit or screener weight change, the same `rm data/.cockpit_cache/*.pkl` then restart sequence is required (it's persisted-to-disk by design).
- **MF holdings have a sibling-propagation invariant**: 1 ETMoney URL → ~2.6 AMFI scheme_codes via shared `etm_id`. If you migrate to a different holdings source, check that the new source preserves this 1:N mapping or the `mf_holdings` writes will multiply.

## Active plan
[plan 0002 — 100 factors and model](docs/plans/0002-100-factors-and-model.md) §3.2 (factor build) and §3.3 (factor-model upgrade) just got an informal advancement: **2 candidate factors wired** (pt_upside, eps_growth) + **2 weight-optimization objectives shipped** (RETURN, SHARPE) ahead of the formal §3.3a/b/c phases. MF plan [prfect-lets-add-a-zazzy-eich](.claude/plans/prfect-lets-add-a-zazzy-eich.md) Phase 4 complete (holdings now auto-scraped); ready to archive next session.

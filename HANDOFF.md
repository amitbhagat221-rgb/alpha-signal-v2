# HANDOFF
Updated: 2026-05-24 (session #4 of day) | Branch: master (will be 1 unpushed after commit) | HEAD: prior `ca8c11a`

## Left off
Shipped **plan 0005 Phase A + Phase B** end-to-end ([ADR 0024](docs/decisions/0024-per-signal-eligibility-and-per-stock-integrity.md)) — the two highest-leverage steps in the 75 → 95 data-confidence climb. Phase A: `eligibility/registry.py` declares per-signal `eligible_universe_sql` for 8 production signals, `universe_eligibility` table records (sid, signal, snapshot_date) → eligible, `tools/refresh_eligibility.py` populates nightly (wired before screener in config.PIPELINE_STEPS), and `scoring/screener.py` now computes `eligible_coverage` alongside `weight_coverage` so a SMALL cap missing consensus that was never going to have it gets full credit. Phase B: `validators/per_stock_integrity.py` runs 8 cross-source assertions (the HALC catcher among them) on every SID in daily_picks; FAIL bumps the SID from `cockpit.api.get_top_picks` (which drives morning_brief + action_queue). **First run already paid off** — caught 14 stocks ranked with `consensus_signal != NULL` but `total_analysts = 0` (the previous gate used `IS NOT NULL` which passes for `0`); fix landed in same commit. Cockpit Health Center got two new sections: "Universe coverage per signal" (grid with per-signal eligible/ineligible bars) and "Per-stock integrity violations" (FAIL/WARN list with explorer drill-down), plus a new 5th pillar tile "Picks integrity". Confidence estimate now ~85/100 (was 75).

## Pick up here
1. **Start [plan 0005 Phase C — coverage gap closure](docs/plans/0005-data-confidence-to-95.md#phase-c--coverage-gap-closure-85--88)** — with A+B in place, the eligibility data is screaming where the real gaps are: `consensus` only 976/2448 (40%) eligible (yfinance ceiling), `momentum` 1970/2448 (80%) missing prices for the SME tail, `book_to_price` 2106/2448 (86%) missing shares-outstanding. Phase C targets each: BSE bhavcopy fallback for 339 priceless SIDs + yfinance ticker audit + regulatory feed recovery. 2-3 sessions.
2. **Watch the daily for a few runs**, then **switch the pick gate to use `eligible_coverage`** (currently both populate but only `weight_coverage` is the gate). The behavioural change is small (a few hundred more LARGE/MID caps qualify) but the principle is right. ~1hr once we're confident.
3. **Extend `validators/per_stock_integrity.py`** with one more assertion: `market_cap = shares_outstanding × close × 1e-7` (cr conversion) within 5%. We have `market_cap_cr` in `stocks` and the components in `annual_balance_sheet` + `stock_prices`. ~30min, would close another HALC-class hole.

## Watch out
- **Two coverage metrics** in daily_picks now: `weight_coverage` (legacy, denominator = tier total weight) and `eligible_coverage` (denominator = eligible weight only). They'll be close for SMALL (smart_money/promoter are 99% eligible) but diverge for LARGE (consensus is 40% eligible). When reading daily_picks, pick one deliberately. Plan: deprecate `weight_coverage` after Phase A.5 (gate switch).
- **`eligibility/registry.py` SQL is opinionated** — eligibility thresholds (e.g. "≥4 quarters of EPS" for earnings_yield) are encoded inline. If you tune them, the eligible/ineligible counts shift and the cockpit eligibility grid moves. Document any change in the registry's docstring.
- **`validators/per_stock_integrity.py` runs synchronously inside the screener** on ~1800 picks; takes ~1s today. Adding more assertions or growing pick count past 5000 may slow this. If it crosses 10s, batch the cross-source SELECTs (currently 5 separate queries per call).
- **Phase B catches LIVE bugs**, not just historical. Tomorrow's pipeline run will re-validate; if a new producer regression introduces inconsistency, the integrity_status column will fire CRITICAL in cockpit Overview before anything reaches morning_brief.
- The **graphify rebuild hook will fire** on the upcoming commit — `~/.cache/graphify-rebuild.log` (off-context; ignore unless it errors).

## Active plan
[docs/plans/0005-data-confidence-to-95.md](docs/plans/0005-data-confidence-to-95.md) — Phases A + B done; Phase C is the named next step. Track 3 plan 0002 §3.1b (NSE F&O OI) still parked behind Phase C (data quality before more factors per ADR 0023 + plan 0005 sequencing rationale).

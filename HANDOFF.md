# HANDOFF
Updated: 2026-05-24 (session #4 of day, post gap-closure) | Branch: master (will be 1 unpushed after commit) | HEAD: prior `62854e0`

## Left off
Shipped **plan 0005 Phase A + Phase B fully** including all 5 gaps from my own honest gap-audit. Earlier in session: A+B at ~80%/~75% with deferrals. Now all 5 closed:
  1. **HALC-injection self-test** in [validators/per_stock_integrity.py](validators/per_stock_integrity.py) — `python -m validators.per_stock_integrity --self-test` runs 10 deliberate injections including the actual 2026-05-22 HALC values; 10/10 pass.
  2. **`market_cap_consistency` assertion** added (function + self-test case), but held OUT of production CHECKS until Phase C unit-normalization (units in `stocks.market_cap_cr` are actually rupees, not crores; `annual_balance_sheet.shares_outstanding` is millions, not absolute — full inconsistency. The assertion logic is correct; it just needs unit-normalized inputs to be useful in production).
  3. **FAIL gating across all `output/*` readers**: extended to `output/email_sender.py`, `output/dossier.py`, `output/diff_engine.py`. Now there is no way for an integrity-FAIL pick to reach the daily email, the LLM dossier, or the rank-change diff.
  4. **Dossier validator narrative-vs-structured cross-check** in [output/dossier.py](output/dossier.py): when a number in narrative *appears* to quote a structured field (e.g. "16.5% downside" near "downside" keyword), it's reconciled against the actual `pt_upside`/`current_price`/`target_price`/`pe_ratio`/`roe`. Mismatches escalate to `narrative_contradicts_structured` with structured/claimed/actual/tolerance in the violation record. Verified: HALC injection now reports `narrative_claimed: 16.5, actual: -8.5`.
  5. **Pick gate switched to `eligible_coverage`** in [scoring/screener.py](scoring/screener.py) — `MIN_ELIGIBLE_COVERAGE = 0.60` is the primary gate; `MIN_WEIGHT_COVERAGE = 0.50` retained as backstop (defence in depth in case eligibility data is wrong). A SMALL cap that's covered on its 6 ELIGIBLE signals is no longer punished for missing consensus.

End state: 0 CRITICAL, 12 WARN unchanged. Pipeline runs clean. Self-test 10/10. Confidence estimate now ~85/100 — A and B are FULLY done per the plan's "done when" criteria.

**Then continued into Phase C** — 30% done this session:
  • **Regulatory feed recovery** — root cause: `fetch_regulatory` pipeline step called `harvest_all` which is a 3-year historical backfill (180 Google + 870 RBI iterations + 110K PIB IDs). Timed out daily, left stale RUNNING rows, never landed new events → table went 44d dark. Shipped `harvest_incremental(days=30)` in [sources/regulatory_harvester.py](sources/regulatory_harvester.py) — last-30d Google News only, ~5min runtime. Wired into config.PIPELINE_STEPS. Manual backfill landed 1904 new events; raw feed went from latest 2023-05 → 2026-05. WARN count 12 → 11.
  • **`ELIGIBILITY_REGRESSION` sanity check** in [tools/data_sanity.py](tools/data_sanity.py) — compares each signal's eligible count today vs prior snapshot in `universe_eligibility`. Fires WARN at 5% drop, CRITICAL at 10%. Catches "harvester silently shrinking universe overnight" — exactly the bug class Phase C item 5 was designed to prevent. Returns 0 today (need 2 snapshots for the JOIN); gets teeth tomorrow.
  • **Deferred items tackled later in same session (post "lets fix this now")**:
    - **yfinance ticker audit**: probed 10 well-known SMALL caps (Tata Investment, Gillette, Astrazeneca, etc) on both `.NS` and `.BO` — zero have yfinance analyst data. Confirms the 41% LARGE+MID coverage is the yfinance ceiling for Indian sell-side. **No code change**; documented in [sources/yfinance_analyst.py](sources/yfinance_analyst.py) docstring as known limit.
    - **yfinance price fallback** (NOT BSE bhavcopy — yfinance was 10× cheaper): new [sources/yfinance_prices.py](sources/yfinance_prices.py) tries `.NS` then `.BO` for any SID missing from `stock_prices` in last 30d. Wired into PIPELINE_STEPS as `fetch_prices_fallback` after `fetch_bhavcopy`. Manual backfill: **330/333 SIDs filled** (3 via `.NS`, 327 via `.BO`), 9,296 price rows written. Universe coverage jumped 2,109 → 2,445 (86% → 99.9%). Eligibility deltas: earnings_yield 1963→2226 (+263), book_to_price 2106→2443 (+337), smart_money 2100→2446 (+346). WARN count 12 → 10. The 2 SIDs with no yfinance data (DHENUBUILD, ISCITRUST) are truly dark — accept.
    - **Eligibility regression check armed**: seeded 2026-05-23 snapshot manually so `ELIGIBILITY_REGRESSION` now has a baseline to compare against (returned 0 — none of the 8 signals dropped, several increased). Tomorrow's natural cron run extends the history.
    - **Classifier restart**: still deferred — Anthropic budget decision, not code task. The raw `regulatory_events` feed is flowing (24,025 events as of session end), but `regulatory_signals` (the classified table) remains 44d stale until budget-bounded incremental classifier batch runs. Up to user.

## Pick up here
1. **Start [plan 0005 Phase C — coverage gap closure](docs/plans/0005-data-confidence-to-95.md#phase-c--coverage-gap-closure-85--88)** — with A+B in place, the eligibility data is screaming where the real gaps are: `consensus` only 976/2448 (40%) eligible (yfinance ceiling), `momentum` 1970/2448 (80%) missing prices for the SME tail, `book_to_price` 2106/2448 (86%) missing shares-outstanding. Phase C targets each: BSE bhavcopy fallback for 339 priceless SIDs + yfinance ticker audit + regulatory feed recovery. 2-3 sessions.
2. **Watch the daily for a few runs**, then **switch the pick gate to use `eligible_coverage`** (currently both populate but only `weight_coverage` is the gate). The behavioural change is small (a few hundred more LARGE/MID caps qualify) but the principle is right. ~1hr once we're confident.
3. **Extend `validators/per_stock_integrity.py`** with one more assertion: `market_cap = shares_outstanding × close × 1e-7` (cr conversion) within 5%. We have `market_cap_cr` in `stocks` and the components in `annual_balance_sheet` + `stock_prices`. ~30min, would close another HALC-class hole.

## Watch out
- **Two coverage metrics** in daily_picks now: `weight_coverage` (legacy, denominator = tier total weight) and `eligible_coverage` (denominator = eligible weight only). They'll be close for SMALL (smart_money/promoter are 99% eligible) but diverge for LARGE (consensus is 40% eligible). When reading daily_picks, pick one deliberately. Plan: deprecate `weight_coverage` after Phase A.5 (gate switch).
- **`eligibility/registry.py` SQL is opinionated** — eligibility thresholds (e.g. "≥4 quarters of EPS" for earnings_yield) are encoded inline. If you tune them, the eligible/ineligible counts shift and the cockpit eligibility grid moves. Document any change in the registry's docstring.
- **`validators/per_stock_integrity.py` runs synchronously inside the screener** on ~1800 picks; takes ~1s today. Adding more assertions or growing pick count past 5000 may slow this. If it crosses 10s, batch the cross-source SELECTs (currently 5 separate queries per call).
- **Phase B catches LIVE bugs**, not just historical. Tomorrow's pipeline run will re-validate; if a new producer regression introduces inconsistency, the integrity_status column will fire CRITICAL in cockpit Overview before anything reaches morning_brief.
- The **graphify rebuild hook will fire** on the upcoming commit — `~/.cache/graphify-rebuild.log` (off-context; ignore unless it errors).

## Active plan
[docs/plans/0005-data-confidence-to-95.md](docs/plans/0005-data-confidence-to-95.md) — Phases A + B done; Phase C is the named next step. Track 3 plan 0002 §3.1b (NSE F&O OI) still parked behind Phase C (data quality before more factors per ADR 0023 + plan 0005 sequencing rationale).

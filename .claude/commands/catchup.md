Read HANDOFF.md. Read the active plan(s) it references in docs/plans/.
Run `git status` and `git log --oneline -10`.

Then summarize in 5 short sentences:
1. Where I am
2. What's in flight
3. What's next
4. What's blocking
5. What you'd suggest doing in this session

If HANDOFF.md is older than 48 hours or git is dirty in unexpected ways,
flag it. Don't propose work yet — just give me the situation report.
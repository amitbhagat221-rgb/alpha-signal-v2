# Alpha Signal v2 — Progress Checklist
_Last updated: 2026-06-04 (**Sector-signal + multibagger-HOLDING session ([plan 0008](0008-multibagger-model.md), [ADR 0040](../decisions/0040-multibagger-holding-not-selection.md)): validated at-entry sector signals (`tools/sector_signal_lab.py`, 40 monthly × 11 GICS) — sector momentum works 1–6mo (t+3.0–4.3) but DEAD by 2–4yr; macro engine orthogonal→ensemble t+3.0; commodity-momentum→Metal/Energy t+2.2; RBI-cut→rate-sensitives FAILS (cuts into stress). Multibagger SELECTION confirmed dead (`--sector-decomp`: stock-score ρ−0.06, SECTOR ρ+0.41 but unpredictable@2–4yr) → **REFRAME to HOLDING**: 81% of 3x+ winners draw ≥30% (split-adjusted) → stops eject winners (−30% 1.88→1.56x); conviction monitor (HOLD/WATCH/REVIEW, `tools/multibagger_monitor.py`) retains 94–100% of winners, wired `/multibagger`. Built 3 monthly PIT accumulators (`sector_analyst_breadth_pit`/`sector_sentiment_breadth_pit`/`sector_policy_pit`, 803 rows backfilled). Commits `8516504`/`885e8d1`/`5bbe322`.**) Earlier 2026-06-03 (**Multibagger model session ([plan 0008](0008-multibagger-model.md), [ADR 0039](../decisions/0039-multibagger-funnel-regime-dominated.md)): built `signals/gross_profitability.py` (Novy-Marx anchor, full contract+PIT) + `signals/multibagger.py` 3-stage funnel → `multibagger_scores` (35 survivors: TIPS/Steelcast/Manyavar); survivorship-corrected + split-adjusted cohort study (`tools/build_historical_universe.py`+`historical_universe` via bhavcopy archive, `tools/multibagger_cohort.py`, `corporate_actions` backfilled to 2018). TWO-REGIME finding: multibagger capture is REGIME-DOMINATED (quality +0.10x 2018→21 bear, −0.30x 2022→26 rally). Validation-stage, NOT wired. Earlier same day: Horizon-gate weight-review session (commit `a8ecd4b`): acted on the gate's live re-eval (Next-3 #1). Fixed `promotion_gate._live_keys()` alias bug — live re-eval was scoring 6 of 12 wired factors, now all 12 (24 deployed (signal,tier) pairs, 11 clear). Deliberate review (gate × v1-history agreement, never mechanical): LARGE dropped `momentum` (t=0.00, broke t<0.5→0× + gate REJECT), equalized `accruals`/`piotroski`→0.09 (diversifiers not alpha), `earnings_yield` 0.15→0.12, `consensus` 0.30→0.35, `book_to_price` 0.08→0.10; SMALL `pledge_quality` 0.13→0.10 (gate LIBRARY/single-horizon-fragile) → `book_to_price` 0.12; MID unchanged (flags are gate↔history conflicts). Follow-ups: surfaced `factor_horizon_gate` on `/system` Promotion Funnel (verdict tiles + flagged chips); registered+backtested `smart_money_score` (was wired 0.06 with ZERO backtest — verdict SMALL t=1.06 n=6 DROP / gate slow-126d PROMOTE-but-thin → held; fixed 4 alias maps mis-pointing it at avg_delivery). Σ=1.0, screener clean, 0 CRITICAL.**) Earlier 2026-06-02: **Triple session ([ADR 0038](../decisions/0038-horizon-resolved-promotion-gate.md)): horizon-resolved net-of-cost promotion gate shipped — [tools/promotion_gate.py](../../tools/promotion_gate.py) + `factor_horizon_gate` table judge every factor at its cost-resolved natural horizon. First read PROMOTE 36 · LIBRARY 40 · REJECT 163; 8/18 live (signal,tier) clear the bar; pt_upside self-corrected to FAST 5d (dodged the 252d artifact). Plus §3.2.6 `industry_id` control + §3.2.7 macro betas (oil/metals/inr/gold, per-stock β not levels) — metals/gold LARGE WEAK, rest DROP, all benched. Plus ADR 0037 follow-up: FACTOR_UPSTREAM_TABLES repointed at verdict-bearing derived tables (piotroski_scores) so per-sid UHS plausibility resolves for 734 analyst-thin SMALL picks. Commits 2978d04/77f71b5/f2eb28e.**) Earlier 2026-06-01: **Evaluation-methodology session ([ADR 0036](../decisions/0036-horizon-resolved-factor-evaluation.md)): IC-decay diagnostic shows factors have heterogeneous natural horizons — single-20d t-stat is the wrong lens for slow factors; read-only, no model change. pick_outcomes → trading-day windows 20/63/126; benchmark fetch wired (was silently 32d stale); /system 14-CRITICAL false alarm fixed (expected-empty quarantine tables). Commits 4606f9b/f94d067/58800e6/0fb4ef1.**) Earlier 2026-05-31: **Promotion wave: pt_upside/pledge_quality/delivery_anomaly_z idle→production** after orthogonality sweep (eps_growth held, redundant); LARGE 7/MID 8/SMALL 10 factors. Earlier: §3.2.5 PEAD built (4/6) — core PEAD did NOT replicate (no announce dates+consensus); only `corporate_action_density` LARGE KEEP (unclear mechanism, benched). Earlier same day: `iv_skew_25d` PROMOTED → live MID factor (first prod promotion); 6 daily microstructure (`kyle_lambda` KEEP-benched); §3.2.2 fully done (8/8); 12mo fno_bhav backfill; Kite scaffold. Manual actions done: cockpit restarted (funnel→85, Phase E badges live) + screener re-ran (picks reflect new weights)) · Plans are truth, this is the view. Update via `/handoff`._
_Glyphs: ✅ done · ⏳ next/in-progress · 🚫 blocked · 💤 parked · ↔ cross-track integration point_
_Convention: [ADR 0015](../decisions/0015-track-numbering-and-rename.md) (tracks) + [ADR 0016](../decisions/0016-plan-numbering-fresh-start.md) (plans)._

## Next 3
0. ✅ **Sector signals validated + multibagger HOLDING reframe (2026-06-04)** ([plan 0008](0008-multibagger-model.md) Phase 5 · [ADR 0040](../decisions/0040-multibagger-holding-not-selection.md)) — **At-entry sector signals validated** ([tools/sector_signal_lab.py](../../tools/sector_signal_lab.py), 40 monthly × 11 GICS — far more power than the 7 multibagger windows): sector momentum works 1–6mo (ρ+0.17→+0.26, t+3.0–4.3) but decays to ρ≈0 by 36mo; `macro_sector_signals_pit` weak alone (t+2.0) but orthogonal (corr+0.08)→**ensemble t+3.0**; commodity-momentum→Metal/Energy t+2.2 ([tools/sector_regime_history.py](../../tools/sector_regime_history.py), 15yr); RBI-cut→rate-sensitives FAILS (cuts coincide with stress; the tradeable rate signal is global US10Y, not repo). **Multibagger SELECTION confirmed dead** (`multibagger_cohort.py --sector-decomp`: stock-score ρ−0.06 vs forward multiple, SECTOR realised ρ+0.41 dominates but unpredictable@2–4yr). **REFRAME → HOLDING**: split-adjusted path study ([tools/multibagger_monitor.py](../../tools/multibagger_monitor.py)) — 81% of 3x+ winners draw ≥30% en route → stops eject winners (−30% stop 1.88→1.56x across 2 windows); winners −41%/10mo vs losers −62%/27mo underwater; **conviction monitor** (sell only on the full loser signature) retains 94–100% of winners ≈ buy-hold. Per-name **HOLD/WATCH/REVIEW** wired to `/multibagger` ([cockpit/api.py](../../cockpit/api.py) `_conviction_verdicts`, split-adjusted). Built **3 monthly PIT accumulators** ([signals/sector_breadth.py](../../signals/sector_breadth.py), [signals/sector_policy.py](../../signals/sector_policy.py); `sector_analyst_breadth_pit`/`sector_sentiment_breadth_pit`/`policy_events`+`sector_policy_pit`, 803 rows backfilled) wired as monthly steps → at-entry sector signals backtestable in ~12mo. Commits `8516504`/`885e8d1`/`5bbe322`. **→ next:** (a) ✅ restarted `alpha-cockpit` — conviction column live (2026-06-04); (b) ⏳ doc + wire the daily sector-momentum tilt into `daily_picks` (validated t+3, NOT built — needs its own ADR); (c) ✅ **bear stress test done (2026-06-04)** — `tools/multibagger_monitor.py --sector-stress` replays the live eject rule on 15yr of NSE sector indices; un-guarded REVIEW is INVERTED in real bears (fires at 2008/COVID capitulations that then ~doubled, REVIEW fwd24 +110% vs HOLD +27%) → shipped a **market-regime guard** (suppress REVIEW when NIFTY SMALLCAP 250 ≥20% off peak; eject only idiosyncratic weakness) in `cockpit/api.py:_conviction_verdicts` (0 REVIEWs in bear months now, Realty-2011 preserved) + detailed in-app usage guide on `/multibagger` ([ADR 0040](../decisions/0040-multibagger-holding-not-selection.md) caveat resolved). **Residual follow-up:** idiosyncratic-laggard-in-recovery (PSU Bank H2-2020 → +122%) still mis-flags — no regime guard catches it → REVIEW reframed as "reassess, not sell"; revisit if a true per-stock pre-2022 path source appears.
0. ✅ **DRIVE-BY (2026-06-04): cleared the health board before regime work — 2 CRITICAL + 2 WARN → 0/0** (`/catchup` surfaced them). **(1) `gross_profitability` lineage CRITICAL** ("lineage before ranking", ADR 0027): shipped into `BACKTEST_SIGNALS` in `43cf21a` (multibagger anchor) with no `FACTOR_LINEAGE` entry → registry coverage 1/92. Added the entry in [lineage.py](../../lineage.py) (Fundamentals_screener family, `_fund([Sales, Raw Material Cost, Change in Inventory, Power and Fuel, Other Mfr. Exp, Total], n=3)` + `_stocks`, Financials-excluded); `missing_factors()` → []. **(2) `insider_trades` STALE 33d + `watchdog_insider_trades_heal` failed 2 days**: NOT a fetcher break — the NSE PIT endpoint is healthy (Mar 2057 rows) but disclosures LAG the trade by weeks, so by-trade_date counts taper monotonically toward today (Mar 2057 → Apr 276/68 → May 1-5: 3 → 0) and the recent ~6wk window is structurally sparse, backfilling later. Fix: producer window `months=1→2` ([sources/nse_insider.py](../../sources/nse_insider.py)) so late disclosures actually land via INSERT OR IGNORE; `STALENESS_OVERRIDE` 30→45 ([db.py](../../db.py)) to match the measured lag. **(3) `earnings_calendar` STALE 6d, no producer**: was a one-off v1-CSV import (notebook); v1 CSV itself dead (mtime 2026-05-10) → cockpit upcoming-events widget empty. Wired a real daily producer `compute_earnings_calendar` on nselib `event_calendar_for_equity` ([sources/nselib_pull.py](../../sources/nselib_pull.py), −3d→+30d, idempotent on UNIQUE(symbol,date)) + `PIPELINE_STEP` ([config.py](../../config.py)) + override 10; 28 rows, 20 upcoming restored. **(4) phantom watchdog streak**: the heal-streak detector keys off each step's *latest* status, but the watchdog never logged a recovery when a table went FRESH on its own (late data / threshold correction) → CRITICAL persists until old FAILED rows age out. Added `_log_recoveries()` ([tools/freshness_watchdog.py](../../tools/freshness_watchdog.py)) — symmetric heal-SUCCESS marker for self-recovered tables (caught insider_trades + pick_outcomes). **Health: 0 CRITICAL / 0 warn, 76 fresh, watchdog healed=3.**
0. ✅ **Multibagger model — regime gate BUILT + VALIDATED (2026-06-04)** ([plan 0008](0008-multibagger-model.md) · [ADR 0039](../decisions/0039-multibagger-funnel-regime-dominated.md)) — `scoring/regime_smallcap.py` (NIFTY SMALLCAP 250 EMA50/200; **index backfilled to 2016**, 2528 bars) + regime-conditioned `PILLAR_WEIGHTS_BY_REGIME` wired into [signals/multibagger.py](../../signals/multibagger.py) (+`smallcap_regime`/`regime_favorable` cols). 3rd window (2019→2022) added; `multibagger_cohort.py` gains `--all-windows` (weight-sweep: winner flips by regime) + `--validate-flag`. **Flag made RIGOROUS, not hand-set**: EMA regime at 8 anchors vs realised forward edge → UPTREND mean −0.27x (validated unfavourable), NEUTRAL≈0, DOWNTREND n=1 inconclusive. **VERDICT: ranking edge is zero-to-negative in EVERY regime → fails plan 0008 "Done when".** Value is the GATES (junk-stripped watchlist), not the ordering. **Phase 3/4 deliberately NOT pursued** (don't over-build a ranked product that didn't validate). Current live regime = UPTREND (weak) → unfavourable. Stays validation-stage, OUT of `daily_picks`.
0. ✅ **DRIVE-BY (2026-06-03): monthly snapshot cron silently failing + freshness blind-spot closed** — `30 4 1 * *` `analyst_consensus_snapshots` cron lacked `cd $PROJECT` → `ModuleNotFoundError: No module named 'sources'` every run (June 1 confirmed; never succeeded — 2026-05-01 rows were a manual backfill). **Root cause class**: standalone-cron tables (outside `PIPELINE_STEPS`) get NO freshness benchmark → invisible to watchdog/health/email; trust gates only validate rows that exist, not absence. Same gap that bit `nse_index_history` 2026-06-01. **Fixed**: (1) cron `cd` added (backup `/tmp/cron_backup_20260603.txt`); (2) recovering June anchor via `--snapshot` (writes `snapshot_date=2026-06-01`); (3) **registered the 4 standalone-cron tables in `config.RAW_TABLES`** — `analyst_consensus_snapshots` (monthly) + `surveillance_flags`/`fii_dii_cash_flow`/`fii_dii_positioning` (daily, via run_daily_forward.sh) — with tuned `STALENESS_OVERRIDES` (snapshot 40, fii_pos 6, cash/surveillance 5). (4) CLAUDE.md rule added (any cron `python -m` must `cd` first); (5) **`short_selling_data` wired into run_daily_forward.sh** (`pull_short_selling(months=1)` in the daily_forward branch) + registered in RAW_TABLES (override 7) — recovered 5/24→6/03 (177 rows). **June snapshot recovered**: 907 rows @ 2026-06-01 (now May+June = 2 anchors; cron-fixed July 1 → 3 → `pt_upside` re-verify back on track for Aug). All 5 tables verified FRESH; **health 68→73 fresh, 0 CRITICAL, no false STALE**. **Left N/A by design**: on-demand PIT panels (`daily_snapshots_pit`), internal logs, frozen v1 archive. **Also fixed (smoke-test find)**: `pull_surveillance_today` F&O-ban parse threw `'int' object has no attribute 'strip'` — `fno_security_in_ban_period` returns a bare list of symbol strings (`['AMBER','KAYNES']`), so `pd.DataFrame(out)` gave an int column label that `.strip()` choked on. Rewrote to normalize list-of-str / list-of-dict / DataFrame shapes; now ingests the ban list (AMBER/KAYNES landed with sids, flag_type=FNO_BAN — first FNO_BAN rows ever, the parse had always crashed). Added a "0 symbols today" print so a clean day isn't silent.
1. ⏳ **§3.2.7 betas at natural horizon** — gate PROMOTEs `gold_beta` LARGE (126d, net_t 4.7) + `metals_beta` MID (126d, net_t 2.7), vs raw-20d WEAK ([config.py](../../config.py) `FACTOR_LIBRARY`). n≈19 thin → re-check after more monthly anchors before wiring.
2. ⏳ **`pt_upside` artifact re-verify (due 2026-08)** — CAPPED 0.16–0.25 in `config.SIGNAL_WEIGHTS`. Gate already self-corrected it (resolved to FAST 5d, not the 252d artifact). `python -m tools.backtest_pit --signal pt_upside` once ≥3 fresh `analyst_consensus_snapshots` exist → un-cap or pull.
3. ⏳ **MID conflict re-judge** — `accruals`/MID (0.19, v1 t=3.20 vs gate REJECT) + `consensus`/MID (0.09) held this session (gate↔history conflicts, never override 36mo history on one read). Revisit via [tools/promotion_gate.py](../../tools/promotion_gate.py) `--reeval-live` once monthly anchors thicken; `smart_money_score` (n=6) firms up the same way. _(Kite + §3.2.3 intraday still queued.)_
4. ⏳ **DRIVE-BY (2026-06-04): forensic blind to steady-state revenue fraud — Rajesh Exports (REXP) miss** — SEBI interim order 2026-06-03 alleges **₹15.15 lakh cr fabricated revenue FY21–25 (~99.8% of consolidated)** at REXP; promoter barred. Our stack **endorsed** it: Beneish M=CLEAN (−2.55), Altman Z=13.7 SAFE, forensic penalty 0, Piotroski 6/9, accruals_signal 0.76, **in `daily_picks` rank 79, UHS 90 TRUSTED** (all confirmed in DB). Root cause: every model we have (Beneish/Altman/Sloan/Piotroski + the un-built Montier-C) is a **YoY-change or distress detector**; REXP is 5yr steady-state, internally-consistent fraud with **fabricated CFO + shrunk receivables (₹11,304→4,933cr FY24→25)** → defeats every accrual/receivables/cash signal, and zero net income neutralises the TATA accrual term. We hold **consolidated-only** statements (fraud lived in the Swiss subsidiary; standalone was tiny) so no standalone-vs-consol reconciliation. **Prototype run (this session, read-only):** scaling gate `TTM revenue / total_assets` over 1,526 non-fin (≥₹100cr assets) → **REXP = 26.5× turnover at 0.01% net margin, rank #1, alone above 12×** with real scale; legit high-turnover names (Redington/Quess/TeamLease/Gokul Agro) sit 5–6× with 1–3% margins. **Discriminator that isolates REXP cleanly = turnover>8× AND |net_margin|<~0.5%.** Fixes: **(1) ✅ SHIPPED+VERIFIED 2026-06-04** — revenue-plausibility **hard exclusion**, tier-agnostic. NOT in quality_gate.py (discovered it's **print-only + SMALL-only** — its EXCLUDED verdicts never reach `daily_picks`; the screener never consumes `gate_status`); wired into the **screener pick-gate** instead (`_pick_eligible`, the one chokepoint that actually drops from `daily_picks`). New [config.py](../../config.py) `REVENUE_PLAUSIBILITY` (turnover>8× **AND** |net_margin|<0.5%, ₹100cr asset floor, financials exempt) + [signals/revenue_plausibility.py](../../signals/revenue_plausibility.py) (pure predicate `flag_revenue_implausible` + universe frame) + [scoring/screener.py](../../scoring/screener.py) merge in `_load_signals` + AND into `_pick_eligible` + non-silent `EXCLUDE` log + [tests/test_revenue_plausibility.py](../../tests/test_revenue_plausibility.py) 9/9. Zero new data; hard exclusion not a weighted factor → no t-stat gate ([ADR 0017](../decisions/0017-factor-library-two-tier-registry.md)). **Flags exactly 1/1,756 (REXP); production rerun dropped it rank 79 → ABSENT, 1693 picks, ranks dense.** _(Committed `e4796c3`, tests 9/9. Latent gap surfaced: the "small-cap quality gate" Track 2.1 is wired NOWHERE into picks — separate follow-up.)_ **(2) ⏳ Dechow F-score** IF we add employee count (non-financial-divergence term survives steady-state fraud — the one literature model with a real shot). **(3) ⏳ standalone-vs-consolidated revenue divergence flag** (needs a standalone pull) — canonical detector for subsidiary-located revenue fraud.

✅ **Done this session (commit `a8ecd4b`):** acted on the gate's live re-eval (Next-3 #1) — deliberate LARGE cleanup + SMALL trim, MID held; surfaced `factor_horizon_gate` on `/system`; registered+backtested `smart_money_score`. See Track 3.3a section + [signal-weights.md](../reference/signal-weights.md) horizon-gate review.

## Track 3.3a — horizon-gate weight review (acted on the live re-eval, 2026-06-03, commit `a8ecd4b`)
- ✅ **Fixed `promotion_gate._live_keys()` alias bug** — it defined `_LIVE_ALIAS` but never applied it, so `--reeval-live` scored only the 6 factors whose config key == registry id (the "8/18 live" headline). Now maps all 12 wired factors → 24 deployed (signal,tier) pairs, **11 clear** the net-of-cost bar. ([tools/promotion_gate.py](../../tools/promotion_gate.py))
- ✅ **Deliberate weight review** (gate × v1-history agreement; act only where BOTH lenses agree, hold conflicts). **LARGE**: dropped `momentum` (t=0.00, broke config's own t<0.5→0× rule + gate REJECT); equalized `accruals`/`piotroski`→0.09 as labelled diversification ballast (both gate-REJECT, kept to avoid over-concentrating consensus); `earnings_yield` 0.15→0.12 (weak-in-LARGE); `consensus` 0.30→0.35 + `book_to_price` 0.08→0.10 (the doubly-validated pair). **SMALL**: `pledge_quality` 0.13→0.10 (gate demotes to LIBRARY, single-horizon fragile) → `book_to_price` 0.12 (gate's strongest factor). **MID unchanged** — `accruals`/0.19 + `consensus`/0.09 are gate↔history conflicts (v1 strong, gate REJECT); never override 36mo on one read. Σ=1.0 all tiers, screener clean, 0 CRITICAL. ([config.py](../../config.py), [signal-weights.md](../reference/signal-weights.md))
- ✅ **Surfaced `factor_horizon_gate` on `/system`** (ADR 0038 "Next" follow-up) — Promotion Funnel card now carries a "Horizon-Resolved Net-of-Cost Gate" section: PROMOTE/LIBRARY/REJECT tiles + "11/24 live clear" + the 13 flagged (factor,tier) pairs as chips (verdict · net_t@horizon · weight), beside the legacy 20d t-stat. ([cockpit_ops/api.py](../../cockpit_ops/api.py) `get_factor_health`, [system.html](../../cockpit_ops/templates/system.html))
- ✅ **`smart_money` validated** (closes a never-backtested gap) — `smart_money_score` (SMALL 0.06) was wired with ZERO backtest and a wrong `t=2.49` label borrowed from `avg_delivery_pct_30d` via a mis-alias. Registered it (`SIGNAL_COLUMN_MAP` + `BACKTEST_SIGNALS` + `BACKTEST_CADENCE`=monthly + `FACTOR_LINEAGE`) and fixed all 4 alias maps (promotion_gate/cockpit_ops/health_score/confidence). Verdict: 20d backtest SMALL **t=1.06** (n=6, DROP); gate finds a slow-126d PROMOTE (net_t 5.03) but on n=6 with bulk-depth-limited history → **PRELIMINARY, held at 0.06**.

## Track 3.3a / §3.2.6-7 — horizon gate + last factors ([ADR 0038](../decisions/0038-horizon-resolved-promotion-gate.md), 2026-06-02)
- ✅ **Horizon-resolved net-of-cost promotion gate** — [tools/promotion_gate.py](../../tools/promotion_gate.py) + `factor_horizon_gate` table (schema.sql). One objective (net-of-cost annualised IR) drives both horizon choice + verdict, so we never classify off raw peak |IC| (ADR 0036 warning). Cost in IC units = T·c_side(tier)/σ(fwd_h) (T=0.3 default) → shrinks with horizon → fast factors charged more. **First read: PROMOTE 36 · LIBRARY 40 · REJECT 163.** Thesis quantified (slow value at 252d, kyle_lambda all tiers); pt_upside self-corrected to FAST 5d (dodged 252d artifact); **live re-eval 8/18 wired (signal,tier) clear the bar** — sign-unstable earnings_yield/MID + book_to_price/LARGE (LIBRARY), cost-eaten delivery_anomaly_z L/M + iv_skew_25d SMALL (REJECT) flagged for weight review. Evidence surface only — no weight changed (CLAUDE.md never-mechanical). On-demand, not in PIPELINE_STEPS. (commit `f2eb28e`)
- ✅ **§3.2.6 industry_id** (control) + **§3.2.7 macro betas** (oil/metals/inr/gold) — see Track 3.2 lines. Fully wired (PIT helpers, 5 migrations, BACKTEST_SIGNALS, 5 lineage drift-clean, SIGNAL_COLUMN_MAP), 27 monthly anchors reconstructed, backtested. metals/gold LARGE WEAK, rest DROP, all `FACTOR_LIBRARY`. (commit `77f71b5`)

## Drive-by — UHS upstream-table reconciliation ([ADR 0037](../decisions/0037-per-stock-uhs-and-pt-plausibility.md) follow-up, 2026-06-02)
- ✅ **`FACTOR_UPSTREAM_TABLES` repointed at verdict-bearing derived tables** ([scoring/health_score.py](../../scoring/health_score.py)). The per-sid UHS plausibility/consistency dims filtered `trust_verdicts` by RAW financials (quarterly_income, annual_balance_sheet) which carry ZERO verdicts → fundamental-data plausibility (`piotroski_scores`, per-sid for ~1,979 stocks) never reached any pick UHS, and **734/1,440 analyst-thin SMALL picks fell back to the universe mean**. Pointed piotroski/accruals/earnings_yield/book_to_price at `piotroski_scores`; dropped verdict-less quarterly_income from eps_growth. Per-sid plausibility now resolves every tier (verified ADAG/MID + SMALL cohort). Refreshed canonical `daily_picks.uhs_score` (1639 TRUSTED / 44 REVIEW). (commit `2978d04`)

## Drive-by — per-stock UHS + PT-plausibility ([ADR 0037](../decisions/0037-per-stock-uhs-and-pt-plausibility.md), 2026-06-01)
- ✅ **Garbage sell-side PTs killed** — user spotted SPRE ₹3,960 PT (+3474% on a ₹110 stock) shown as TRUSTED. yfinance returns absurd `targetMeanPrice` for thin-coverage small-caps (ABCO 159×, SUDAR 46×…). Nulled **18** live PTs + deleted **198** garbage-derived `consensus_signals` rows. Guards: per-fetch Gate-2 owns fresh (silent source-drop removed so the verdict isn't bypassed); new `sweep_pt_plausibility()` ([yfinance_analyst.py](../../sources/yfinance_analyst.py)) nulls+flags stale stored PTs daily; cockpit display guard; `PT_RATIO_IMPLAUSIBLE` data_sanity backstop.
- ✅ **UHS made per-stock** — `dim_plausibility`/`dim_consistency` were universe-wide (every SMALL pick = identical 90/20/19/20). `_gate_pass_rate` gains a `sid` filter; `rollup_pick_uhs` computes provenance/plausibility/consistency from **this sid's** `trust_verdicts` w/ fallback to the universe mean ([scoring/health_score.py](../../scoring/health_score.py)). `record_pt_plausibility_fail()` ([validators/plausibility.py](../../validators/plausibility.py)) writes per-sid `gate_2=0` on rejection. Cockpit badge now reads canonical `daily_picks.uhs_score` (not the stale `health_score` pick row). **SPRE: 90 TRUSTED → 61 REVIEW**; clean stocks differentiate (ZYDS 91 TRUSTED). (commit `f7e3633`)

## Drive-by — cockpit UX cleanup (2026-06-01)
- ✅ **Removed the Signals page** (useless per user) — route + left-rail + mobile-tab + `signals.html` deleted; mobile bottom-bar slot reused for News. `get_active_signals()` now unused but left in api.py (harmless); `SIGNAL_TOOLTIPS` retained (stock_detail uses it).
- ✅ **Model Variants → subpage tab under Model & Backtest** — collapsed the 3 separate Model rail items into one; added a cross-page `page_nav` macro ([_components.html](../../cockpit/templates/_components.html)) rendering a Backtest · Variants · Pick Outcomes strip on all three `/model*` pages.
- ✅ **News: on-brand hero visual** — dropped the slow/broken scraped `og:image`; hero renders a vibrant gradient + glyph + (lead key-number when present). **Key fix:** the top of the smart-sorted feed is the *freshest* stories, which the classifier hasn't enriched yet (87% of pool enriched but median-8h-old items unclassified → topic/sentiment/key_numbers all NULL), so the visual must not depend on enrichment. Fallback: brand-accent (`#7c3aed`) gradient + 📊 + source name → always visible. Enriched items get topic color + stat. _(Enrichment lag is expected, not a bug.)_ **Extended to ALL grid cards** (not just hero): each `.news-card` now has an 88px `.card-visual` thumbnail (topic gradient + glyph + lead key-number) — every card has imagery, consistent feed.
- ✅ **News: decluttered** — sentiment/source-tier/confidence/sort tucked behind a collapsible **⚙ Filters** toggle (opens auto when a filter is active); search + topic tabs + time-window stay visible.
- ✅ **News: fast-read keyword chips** — added `keywords` (JSON) to `news_enriched` (migration); `news_classifier` LLM now emits 3-5 Title-Case tags + a cheap keyword-only backfill (`--backfill-keywords`) for the ~2,340 already-classified 30d rows (ran in background). Rendered as `#chips` on every card + hero. e.g. `#Tega Industries #Nifty500 #RSI Bullish`.
- ✅ **News: real stock photos** ([sources/news_images.py](../../sources/news_images.py)) — Pexels downloader pulls ~8 finance/markets-themed photos per topic (112 total), resizes to 600px, stores under `cockpit/static/news_img/<topic>/` (gitignored — regenerate via the tool + `PEXELS_API_KEY` in run_pipeline.sh). Cockpit rotates per card by topic + stable article-hash (`_news_image_pool`/`_pick_news_bg`), rendered as a topic-tinted photo behind the glyph/key-number. Graceful fallback to the gradient visual if the pool is empty.
- _(All cockpit-UX items above shipped in commit `385ba89`.)_

## Drive-by — evaluation methodology (2026-06-01, ↔ Track 3.3a, [ADR 0036](../decisions/0036-horizon-resolved-factor-evaluation.md))
- ✅ **pick_outcomes → trading-day windows 20/63/126** (was calendar 5/20/60). 5d dropped (≈3 trading days = noise); windows now match the backtest's `fwd_return_20d` trading-day offset. Recomputed: 20d=4,455 rows; 63d/126d=0 (maturing, ETA 2026-07-06 / 2026-10-02). `pick_outcomes` STALENESS_OVERRIDE 14→35 (shortest window now 20td≈28cal).
- ✅ **IC-decay diagnostic** ([tools/ic_decay.py](../../tools/ic_decay.py)) — read-only fwd_return grid {5,20,63,126,252} → per-(factor,tier) IC term structure + FAST/MEDIUM/SLOW + sign-flip flag + curve graphs. First read: FAST 54 · MED 41 · SLOW 132 · 131 flippers; shapes sensible (delivery_anomaly fast, value_composite slow). Confounds flagged: raw IC grows w/ horizon (SLOW inflated); long-horizon survivorship (pt_upside 0.67@252 = artifact). **No model change.** Proposed follow-up: natural_horizon in registry + net-of-cost-IR gate (ADR 0036).
- ✅ **Benchmark staleness fixed** — `nse_index_history` was manual-only (32d stale → excess NULL) AND untracked by freshness (not a pipeline-output table). Wired `fetch_nse_indices`→`compute_nse_indices` daily step (before compute_pick_outcomes) + STALENESS_OVERRIDE 6 + refreshed (now 2026-05-29). Now 70 fresh, tracked.
- ✅ **Cockpit 63/126 headline display** ([cockpit/api.py](../../cockpit/api.py) `get_pick_outcomes_summary` + model_outcomes.html): `headline_window` = longest matured window, `windows_status` w/ maturing ETAs, decile/TS parameterized off it. Live now (20d); auto-promotes to 63/126 as they mature. Restarted alpha-cockpit. (commit `0fb4ef1`)

## Drive-by — cockpit/email health divergence (2026-06-01)
- ✅ **`/system` showed 14 CRITICAL while email said 0** — root cause = duplicated classifier. `cockpit_ops/api.py:get_health_overview` blanket-flagged every EMPTY table as CRITICAL; the 14 are all expected-empty (10 `*_quarantine` reject sinks → empty = clean, 3 `paper_*`, 1 `uhs_calibration_log`). Fix: shared `health_report.empty_table_severity()` policy (OK quarantine / INFO not-yet-live / CRITICAL unexpected); cockpit consumes it. Now `/system` = 0 CRITICAL · 16 info (was 14C·12I); email unchanged. Restarted `alpha-cockpit-ops`. (commit `4606f9b`)

## Drive-by — DQ hardening (shipped `df3c744`, from data-quality audit 2026-05-31)
- ✅ **Identity Gate parity fix** — `validators/identity_check.py:_verify_moneycontrol` now mirrors the auditor's name-vs-slug-segment logic (substring-either-way OR SequenceMatcher ≥0.55) + MC_SLUG_OVERRIDES allowlist, `stocks.name` passed into the gate; killed the ticker-substring shortcut (false-PASS CERA→`kajariaceramics`, false-QUARANTINE CEATLTD→`ceat`). `tools/recover_quarantined_recos.py` re-evaluates quarantined rows against the slug actually fetched. 19/19 `tests/test_identity_gate.py` pass.
- ✅ **`FORECAST_HISTORY_IS_PRICE_HISTORY` SQL fix** — same-date-join so the (deprecated, audit-trail) check is valid if invoked via `--only`. Live data already clean (~1% residual); the `_extract_forecast_rows` 90-day filter + `FORECAST_HISTORY_NON_YEAREND_PRICE` are the working defenses — this only hardens the backstop.

## Shipped today (2026-05-31)
- ✅ **Promotion wave — idle validated factors → production** ([config.py](../../config.py) `SIGNAL_WEIGHTS`). `pt_upside` (LARGE/MID/SMALL t=7.15/8.40/9.14), `pledge_quality` (SMALL t=5.90), `delivery_anomaly_z` (SMALL t=4.76, n=103) were computed + ranked daily (in screener `SIGNAL_COLS` + the variants) but carried **zero production weight** — now wired. **Orthogonality sweep** (user-requested) gated the set: each promoted factor max |ρ|≤0.27 vs already-wired; **`eps_growth` HELD** (ρ=0.63 w/ consensus → redundant), `kyle_lambda` held (cost-coupled liquidity tilt), contrarian-sign/tiny-n bench factors held. **`pt_upside` CAPPED** (0.16–0.25, well below its t=7–9 share) pending the artifact re-verify (open question, 2026-08). Tiers renormalised Σ=1.0 → LARGE 7 / MID 8 / SMALL 10 factors. Screener smoke-tested: pt_upside coverage L 100/100, M 142/149, S 683/1584 (analyst-thin in small caps → renormalises); pledge 97%, delivery 94%. signal-weights.md updated; 0 CRITICAL.
- ✅ **Track 3.2.5 — 4 event-time/PEAD factors** ([signals/pead.py](../../signals/pead.py)), backtested on the deep panel. **Honest negative: the core PEAD didn't replicate.** `earnings_surprise_std` (seasonal-RW SUE) DROP all tiers; `pead_drift_60d` SMALL t=-1.54 WEAK (reversal, not drift). Root cause = no true earnings-announcement dates or analyst consensus in `quarterly_income` → the time-series SUE + end-date+45 announce-approx are too noisy. `corporate_action_density` LARGE t=-3.67 KEEP (CI strictly <0) but **mechanism unclear** (likely maturity/value proxy) + only 2yr corp-action depth → NOT promoted, verify vs value factors. `buyback_announcement_30d` DROP (too sparse, ~9/date). All 4 bench. Deferred `dividend_change_signal` (brittle text-parse) + `index_inclusion_proximity` (needs historical mcap). Lesson: PEAD needs an earnings-calendar + consensus feed to work. 0 CRITICAL.
- ✅ **`iv_skew_25d` PROMOTED → live MID screener factor** (first §3.2.x factor wired to production). Promotion review of the 2 KEEP candidates: extended the IV backtest to **48 weekly periods (~11mo, multi-regime)** — `iv_skew_25d` MID holds at **t=+3.16 KEEP** (was 4.61/25-period), LARGE t=1.37 / SMALL t=0.17 DROP → MID-only. Colinearity check: **orthogonal** to size/adtv/existing factors (|ρ|<0.15) — genuinely new info. Wired into `config.SIGNAL_WEIGHTS[MID]=0.18` (conservative vs the t-tier's 1.0×, given single-derivative-class novelty + ~11mo vs 36mo history; existing 6 scaled ×0.82, Σ=1.0) + `scoring/screener.py` `_load_signals`/`SIGNAL_COLS` (reads `fno_iv_history` latest-per-stock; F&O-only → non-F&O MID names renormalise). Smoke-tested in-process: 101/149 MID stocks scored on it, high-skew names boosted (SAIL→rank 3). **`kyle_lambda` NOT wired** — ρ(kyle,ln_ADTV)=−0.73 (cost-coupled liquidity tilt, MID IC decaying); held as diagnostic. signal-weights.md updated; 0 CRITICAL.
- ✅ **Track 3.2.3 — 6 daily-derivable microstructure factors** ([signals/microstructure.py](../../signals/microstructure.py)). Off daily `stock_prices` OHLCV (no Kite): 3 clean (`intraday_range_compression`=ATR5/ATR20, `closing_strength_1m`, `opening_gap_freq_1m`) + 3 proxies (`vwap_deviation_5d`=close-vs-typical-price since traded_value ~17% NULL, `bidask_spread_proxy`=Corwin-Schultz, `kyle_lambda`=Amihud illiquidity; prev_close self-computed as lag(close), turnover=close×volume). ~99% coverage (NULLs = <22d-history IPOs + circuit-frozen high==low names). Full wiring (PIT helper `pit_microstructure` + 6 cols + BACKTEST_SIGNALS group "Microstructure" + SIGNAL_COLUMN_MAP + 6 lineage, drift clean); monthly cadence → backtested on the **deep 39-month panel** (years of price history, unlike the F&O 22-25). **`kyle_lambda` LARGE t=+4.24 + MID t=+4.14 KEEP** (CIs strictly >0 — the Amihud illiquidity premium, economically textbook; SMALL t=+1.65 WEAK). Other 5 DROP. `kyle_lambda` is a promotion candidate but cost-coupled (you pay the spread you're compensated for) + likely colinear with size/adtv. **3 true-intraday factors ON HOLD** (need Kite 3.1c). 0 CRITICAL.
- ✅ **Track 3.1b — 4 F&O IV factors, derived in-house** ([ADR 0035](../decisions/0035-fno-iv-derived-from-bhav.md)). The IV half of §3.2.2 was thought forward-only/blocked; it isn't — `sources/fno_iv.py` recovers the EOD IV surface by **Black-76 inversion of the settle prices already in `fno_bhav`** (implied forward via put-call parity, OTM-wing IVs, ±25Δ skew interp), no external feed. **Validated**: NIFTY ATM IV tracks India VIX (our own `macro_history`) to ~0.1-2 vol pts and sits just below it (correct); CE/PE IV match exactly at ATM (parity holds). New `fno_iv_history` table + `compute_fno_iv` daily step; **fno_bhav backfilled 6mo→12mo (250 dates)** so `iv_percentile_1y`'s 1y window fills. New [signals/fno_iv_factors.py](../../signals/fno_iv_factors.py) (4 factors) + `pit_fno_iv` + 4 PIT cols + BACKTEST_SIGNALS/CADENCE/MAP/lineage (drift clean). **Backtest (25 weekly periods, NW3): `iv_skew_25d` MID t=+4.61 KEEP** (IC +0.096, CI [2.28,9.84] strictly >0 — first F&O factor to clear the bar; downside-skew → MID outperformance), `iv_realised_spread` MID t=-1.95 WEAK, `iv_term_structure` MID t=-1.80 WEAK (SMALL "KEEP" t=-4.94 is a 7-period/23-stock ARTIFACT, flagged not promoted; ~20% stock coverage — index-level signal), `iv_percentile_1y` DROP. All on bench; `iv_skew_25d` is a candidate for the deliberate promotion process (wants walk-forward OOS first). 0 CRITICAL.
- ✅ **Track 3.1c — Kite Connect scaffold** ([sources/kite_pull.py](../../sources/kite_pull.py) + [docs/reference/kite-setup.md](../reference/kite-setup.md)), **pending live creds**. Auth/daily-token-refresh (TOTP via pyotp, dated cache) + sid→NSE instrument map + minute-bar backfill + daily VWAP/last-30min aggregates. Deliberately NOT in PIPELINE_STEPS until `--check-auth` verifies a real key. Unblocks the 3 true-intraday §3.2.3 factors (forward-accumulation, ~90d clock).
- ✅ **Track 3.1b — 4 F&O OI factors built + backtested** (§3.2.2 OI half; Next-3 #1 done). New [signals/fno_oi_factors.py](../../signals/fno_oi_factors.py) (injectable-frame core, live+PIT share one path) → `pcr_oi`, `pcr_volume`, `max_pain_distance` (stored-column reads) + `oi_buildup_signal` (4-state regime score from same-expiry day-over-day ΔOI vs Δprice — roll-safe: Δ taken only within one expiry series). Stock-only (`sid IS NOT NULL` drops index underlyings). Full wiring: PIT helper `pit_fno_oi` + 4 cols in `daily_snapshots_pit` (via `_COLUMN_MIGRATIONS`) + PIT_COLUMNS/VALIDATION_RANGES + `BACKTEST_SIGNALS` (group "Options/F&O") + `BACKTEST_CADENCE` weekly + `SIGNAL_COLUMN_MAP`/NW3 + 4 `FACTOR_LINEAGE` entries (no drift). PIT reconstructed over 30 weekly Fridays (207-222 stocks/date from 2025-11-28; correctly NULL for 3 pre-backfill Fridays). **Backtest (22 weekly periods, NW3): `pcr_volume` SMALL t=-1.69 WEAK (high put-vol → mild underperformance, sensible sign), `max_pain_distance` MID t=-1.68 WEAK (mean-reversion to max-pain), `pcr_oi` best |t|=0.36 DROP, `oi_buildup_signal` best |t|=0.45 DROP.** None clear the 2.0 gate (CIs straddle 0 over one regime) → all 4 on the bench (`FACTOR_LIBRARY`), none wired to screener. data_sanity 0 CRITICAL, health green.
- ✅ **Track 3.1b — NSE F&O OI data foundation** ([ADR 0034](../decisions/0034-fno-oi-data-model.md)). New [sources/fno_pull.py](../../sources/fno_pull.py) on `nselib.derivatives.fno_bhav_copy` — **one call returns the entire EOD F&O market** (~35K rows: every strike × CE/PE × expiry × 211 stock + 5 index underlyings), so the plan's per-symbol `option_chain_equities` loop is moot **and** it's archive-backfillable. Two tables in [schema.sql](../../schema.sql): `fno_bhav` (raw grid, kept oi>0∨vol>0 → 16.3K/35.6K rows/day) + `fno_pcr_history` (nearest-expiry PCR + max-pain rollup). **6mo backfill: 2.83M rows / 122 dates (2025-11-27→2026-05-29), 216 underlyings/day**; 211/211 stock underlyings map to sid (indices sid-NULL). Wired `fetch_fno_bhav` + `compute_fno_pcr` daily in `PIPELINE_STEPS`, `STALENESS_OVERRIDE`=6 ([db.py](../../db.py)); both register FRESH/OK. Smoke-verified (NIFTY PCR 0.53, RELIANCE 0.72) + idempotent. **Foundation only — OI factors are next session (§3.2.2, Next-3 #1), IV path deferred pending weekday verify.**
- ✅ **Offsite nightly DB backup → Google Drive** — VM-only `backup_db.sh` (`*.sh` gitignored): `VACUUM INTO` (safe on a live DB) → `integrity_check` → gzip (2.6GB → **534MB**) → rclone to `gdrive:alpha-signal-v2-backups` (scope=drive.file, 5 TiB Drive) → 7-daily/monthly retention. Cron `0 5 * * *` UTC (after pipeline+duckdb+health). Restore-verified (gunzip → plain sqlite, 115 tables, fno_bhav row-count matches). First upload confirmed in Drive.
- ✅ **`corporate_actions` daily wiring** — root cause was the existing [sources/nselib_pull.py:pull_corporate_actions](../../sources/nselib_pull.py) fetcher being in **no cron/pipeline** since May 3, not a missing scraper. Added `compute_corp_actions` (2-mo window) as a daily PIPELINE_STEP + silent-failure raise (all-chunks-fail → RuntimeError) + `corporate_actions: 10` STALENESS_OVERRIDE in [db.py](../../db.py) (anchor is `fetched_at`, flat on holiday clusters). Backfilled 110 May rows; LIC 1:1 bonus + LEM split now resolve via Gate 3's `_has_recent_corp_action` escape hatch. (PRAM was a steady multi-day decline, not a corp action — correctly not suppressed.)
- ✅ **Slug-override registry** — `MC_SLUG_OVERRIDES` in [sources/moneycontrol_recos.py](../../sources/moneycontrol_recos.py): pins verified-correct-but-renamed slugs (DPSC→India Power Corp, CUBEI→Cube Highways InvIT — both autosuggest-unrecoverable), blocks BRIGT (autosuggest returns wrong `brightsolar`) + 4 MICRO dark SIDs (APPA/DED/MER/PUNI, no MC coverage) from re-discovery. [tools/data_sanity.py:_mc_slug_name_mismatch_check](../../tools/data_sanity.py) allowlists the verified set → auditor now flags **0** (was 3).

## Queued
- ✅ **[Plan 0007 — Trust Pipeline + Unified Health Score](0007-trust-pipeline-uhs.md)** — **fully shipped 2026-05-30 across 4 sessions + remediation pass 2026-05-31** ([ADR 0033](../decisions/0033-trust-pipeline-uhs.md)). 7 gates live · per-pick UHS on every daily_picks row · 11+ vocabularies collapsed to one 🟢/🟡/🔴 scale · 9 historic bugs as permanent pre-push regression fixtures · 14 redundant data_sanity checks deprecated. Honest ceiling at our scale = 95/100; true 100 needs paid Bloomberg/Refinitiv.
   - ✅ Phase 1 — UHS schema + score writer · commit `18ecaed`
   - ✅ Phase 2 — Identity Gate (Gate 1) · commit `c901c6e`
   - ✅ Phase 3 — Plausibility + Temporal + Regression Fixtures · commit `d1d0d14`
   - ✅ Phase 4 — Cross-Source + Unit Contract (Gates 4 + 5) · commit `46e6b98`
   - ✅ Phase 5 — Lineage + Pick-Level UHS rollup (Gate 6) · commit `b00374f`
   - ✅ Phase 6 — External Anchor (Gate 7) · commit `9ec28e9`
   - ✅ Phase 7 — Streamlining (38→25 active checks) · commit `ce5cd58`
   - ✅ Phase 8 — Confidence orchestration + ADR 0033 + calibration scaffold · commit `549083d`
   - ✅ Remediation 2026-05-31 — [tools/trust_backfill.py](../../tools/trust_backfill.py) exercises Gates 2/3/4/5/7 against existing rows · surfaced 4 root-cause bugs (Tickertape `.change` field unreliable, autosuggest parser reading non-existent fields, `_mc_slug_name_mismatch_check` ticker-substring shortcut, `rollup_pick_uhs` Phase-1 stub reading 3-of-5 dims) · 46 wrong-entity SIDs quarantined + 43 re-slugged · 35 EPS-growth values NULL'd · `/system` Trust Pipeline card live on port 3001 · pick UHS distribution flipped 1689 PRELIMINARY → 149 TRUSTED + 1440 PRELIMINARY + 100 REVIEW · commit `22f1dcf`
- ✅ **[Plan 0006 — Sector dossiers](0006-sector-dossiers.md)** — `/sectors` front-door rebuild, **fully shipped (A–E)**. A+B+C 2026-05-29; D+E 2026-05-31. Archive in ~30 days.
   - ✅ Phase D — LLM-narrated per-sector thesis. [output/sector_dossier.py](../../output/sector_dossier.py) + `sector_dossiers` table; mirrors `output/dossier.py` no-raw-numbers hygiene (reuses `_scan_for_numbers`). 11 calls/night, 10/11 valid (Consumer Staples leaked "2/3" → valid=0 → cockpit shows `{}`). Rendered under each bucket row: thesis + horizon-tagged what-to-watch + bull/bear/tech disclosure.
   - ✅ Phase E — per-sector S/M/L horizon badges + [signals/sector_momentum.py](../../signals/sector_momentum.py) factor. Constituent cap-weighted RS vs NIFTY 50 (21/63/252d), tercile-classified into `sector_briefs.horizon_{short,medium,long}`. PIT helper `pit_sector_momentum` + BACKTEST_SIGNALS + FACTOR_LINEAGE. **Backtest (29 mo): SMALL t=1.88 WEAK · MID 0.33 · LARGE −0.60 DROP → bench, not wired.**

## Shipped today (2026-05-29, cont.)
- ✅ **Walk-forward OOS validation** — [tools/walk_forward.py](../../tools/walk_forward.py). Fits factor weights (sign+magnitude) on months 1..N of the 35-date v1 PIT panel, tests composite IC on unseen month N+1, rolls forward → ~17-23 non-overlapping OOS periods/tier. **Headline: SMALL is VALIDATED** (OOS mean IC +0.034–0.047, t=2.1–2.5, 95% CI strictly >0, 71–82% months positive; stable across expanding / rolling-18m / min_train=18). **LARGE + MID show ~zero OOS skill** — the live "MID +0.076" (2 overlapping windows) was an artifact, did not survive. **Equal-weight ≈ IC-weighted; best_single worse** → optimize_weights/RETURN/SHARPE variants add nothing OOS over equal-weight. **pt_upside + eps_growth absent from v1 panel → zero OOS validation** (analyst PT episodic, snapshotted only since 2026). ⏳ Next: turn SMALL IC into net-of-cost (150bps) turnover-aware portfolio return before sizing capital. Updated SETUP.md §12/§16.
- ✅ **Track 2.2b-v2 — financial_signal split by direction** ([ADR 0032 proposed](../decisions/0032-tier-direction-flip-split-signal.md), commit `0d8d8bd`). Single-direction Phase 2.2d composite FAILED the done gate (t = -0.75 / -1.30 / -0.34); the diagnostic surfaced opposing signs on NPA (LARGE/MID `net_npa_pct` t=+2.39/+4.16 mean-reverting, SMALL `gross_npa_pct` t=-3.09 quality compounds). Split into [signals/financial_signal.py](../../signals/financial_signal.py)'s `financial_quality` (direction='lower', for SMALL) and `financial_recovery` (direction='higher', for LARGE/MID), sharing profitability/capital/funding components. Schema migrated via `_COLUMN_MIGRATIONS` (financial_signal_scores + daily_snapshots_pit); `financial_signal` kept as back-compat alias = quality. BACKTEST_SIGNALS in `db.py` updated (financial_signal → SUPERSEDED; both new → READY). PIT helper in [tools/reconstruct_pit.py:pit_financial_signal()](../../tools/reconstruct_pit.py) writes all three. **Backtest on 30 monthly PIT anchors**: financial_recovery MID **t=+1.55 WEAK** (mechanism confirmed), SMALL **t=-1.88 WEAK** (confirms quality direction for SMALL); financial_quality MID **t=-0.48** + SMALL **t=+0.44** DROP. Neither clears |t|≥2.0; both stay on bench, NOT routed into screener. Will revisit ~Q1 FY27.
- ✅ **2 non-colinear bench factors wired into screener** (commit `0d8d8bd`). `pledge_quality` (SMALL t=5.90, KEEP) — added to existing `promoter_signals` SELECT in [scoring/screener.py:_load_signals()](../../scoring/screener.py). `delivery_anomaly_z` (SMALL t=4.76, KEEP) — new [signals/delivery_anomaly.py](../../signals/delivery_anomaly.py) mirrors `momentum.py` live-compute pattern (90d z-score of latest delivery_pct vs baseline, clip ±5, 1,999 stocks scored). Both added to `SIGNAL_COLS`. [tools/optimize_weights.py](../../tools/optimize_weights.py) gains `--filter-wired` flag (drops unwired + renormalises per tier, paste-ready). `config.SIGNAL_WEIGHTS_RETURN` + `SIGNAL_WEIGHTS_SHARPE` refreshed: **unwired share LARGE 0% / MID 0% / SMALL 0%**. Variant pick-gate excluded count 251 → **210** (-41) because new factors reduce analyst-coverage dependence in SMALL. `pledge_quality` t=5.90 SMALL + `delivery_anomaly_z` t=4.76 SMALL each get their natural ~12% weight in SMALL variants.
- ✅ **NBFC GNPA fallback probe** (Phase 2.2c, research only, parked). Confirmed gap is data-not-on-source — Screener.in has labels but empty cells for 33/81 NBFCs (REC, IRFC, BAJAJFINSV, JIOFIN sampled). Source survey: RBI XBRL public portal dead (`xbrl.rbi.org.in` HTTP 000); NSE corporate-filings XBRL has `ImpairmentOnFinancialInstruments` + `FinanicalAssets` but no GNPA% tag in NBFC_INDAS taxonomy; NSE quarterly results PDF is the highest-fidelity path (2-3 day build for PDF table extraction). **Parked**: backtest sample size is the rate-limiter, not the missing 33 NBFCs. `financial_recovery` MID at t=1.55 will cross 2.0 with ~6 more periods if the mechanism holds, regardless of NBFC coverage. Re-evaluate after Q4 FY26 data.
- ✅ **DuckDB read-replica + perf wins** ([ADR 0031](../decisions/0031-duckdb-read-replica.md)). New `tools/duckdb_refresh.py` rebuilds `data/alpha_signal.duckdb` (87 MB columnar) nightly after pipeline; `tools/bench_duckdb_vs_sqlite.py` records the win (11-57× on column-scan-heavy SELECTs). New `db.read_sql_fast()` helper routes mirrored-table reads to DuckDB, falls back to SQLite if file missing. SQLite stays the write-side source of truth. **Measured cockpit gains**: `/model` cold-render 5.6s → 1.6s (3.5×) via [cockpit_ops/api.py:get_backtest_roster](../../cockpit_ops/api.py); `/system` cold-render 33.9s → 13.1s (2.6×) via a SEPARATE [health.py:534](../../health.py) `factor_type_conformance` rewrite — consolidated N per-column scans into 1, sample tables >500K rows at 200K LIMIT (CPU-bound `typeof()` was the bottleneck; not a DuckDB candidate because the check is fundamentally about SQLite's dynamic-typing surprises). Also fixed pre-existing latent circular-import bug in [cockpit_ops/app.py](../../cockpit_ops/app.py) surfaced by the cockpit restart.
- ✅ **Plan 0006 Sector dossiers — Phases A + B + C** ([plan](0006-sector-dossiers.md)). Phase A: [signals/sector_briefs.py](../../signals/sector_briefs.py) + new `sector_briefs` table — rolls macro_sector_signals + daily_picks + regulatory_signals into one row per sector per date with a 4-bucket classifier {BOOMING/LIKELY/HEADWIND/QUIET}. Today: 0/4/1/6. Phase B: [signals/sector_forces.py](../../signals/sector_forces.py) + new `sector_force_breakdown` table — 33 rows/day across 3 forces (macro 10+/1−, regulation 7+/2−, tech 11+/0−; market reserved for v2 — v2 `fii_dii_cash_flow` is index-level only). Phase C: [cockpit/api.py:get_sector_digest()](../../cockpit/api.py) + Tab "Today" rewrite in [cockpit/templates/sectors.html](../../cockpit/templates/sectors.html). 47-card heatmap deleted (`grep -c "ind-card"` now 0). Conflicts-first ordering surfaces Energy ("model still picking here — RELIANCE, BPCL, IOC") at top. Drill-down `/sectors?sector=X#per-sector` unchanged.
- ✅ **Rank-skill gate** — [tools/validate_rank_skill.py](../../tools/validate_rank_skill.py). The go/no-go test before deploying own capital: per-tier top-vs-bottom decile spread on **non-overlapping** windows with a 95% band; never prints "PROVEN" under 6 independent periods. Current read: only **2 independent 20d windows** exist (2026-04-09, 2026-05-07) — LARGE −1.03pp, MID +0.12pp, SMALL +0.39pp, all UNPROVEN. **Do not deploy capital yet.** Re-run weekly as `pick_outcomes` accumulates; invest a tier only when its independent spread's 95% range clears 0 on ≥6 periods.

## Shipped today (2026-05-29)
- ✅ **Factor correlation diagnostic** — [tools/factor_correlation.py](../../tools/factor_correlation.py). Findings: 5 clusters per tier at |ρ|≥0.6, including a "is this a strong business" mega-cluster of 8-12 factors (roe / roa / roic / interest_coverage / debt_to_equity / fcf_margin / fcf_yield / profit_margin / z_score / quality_composite). Killed 5 of 7 proposed wirings as colinear. Output: `data/factor_correlation_{LARGE,MID,SMALL}.json` + `output/factor_correlation_report.txt`.
- ✅ **Live equity curve** — new `pick_outcomes` table + [tools/compute_pick_outcomes.py](../../tools/compute_pick_outcomes.py) + cockpit [/model/outcomes](../../cockpit/templates/model_outcomes.html). 76K outcome rows computed across 5/20d windows × 2,000 stocks × 50 dates. Headline: 20d top-10 baskets show LARGE +2.44% / +2.69pp vs NIFTY 50; MID +3.81% / +0.52pp; SMALL +4.11% / -0.41pp vs SMALLCAP 250. Rank-decile spreads tiny (LARGE +0.04pp · MID +1.69pp · SMALL +0.35pp) — model's rank ordering not yet validated as predictive at 9 dates per tier. Wired into PIPELINE_STEPS daily.
- ✅ **Health drive-by** — dossier sentiment regex tightened (M&M CRITICAL cleared); watchdog CHECK constraint widened + heartbeat row on clean scans (139h-stale WARN cleared). Commit `2a8f299`.
- ✅ **Cockpit restart + spot-check** — `/model` (was HTTP 500) now serves 313KB in 6.7ms warm; `/model/variants` divergent stars rendering; `/mutual-funds/122639` Holdings tab present.

## Track 1 — Foundation  ✅ done 2026-05-01
Audit + tier infra + stratified backtest + 36mo PIT + cutover. See ADRs 0009-0014.

## Track 2 — Portfolio  · [plan 0001](0001-mother-plan.md)
- ✅ 2.1 Small-cap quality gate
- ⏳ 2.2 Financial sub-model — **source decision flipped 2026-05-29: Screener.in, not Tickertape** ([ADR 0030](../decisions/0030-banking-metrics-screener-first.md)). Probe showed Tickertape carries no banking-specific ratios; Screener.in stock pages have GNPA/NNPA/NII/Interest/Deposits. Scope clarified to **158 Banks+NBFCs** (the 91 AMC+Insurance+Capital-Markets stay on main screener).
   - ✅ Phase 2.2a-i: probe + ADR + `banking_metrics` table (28 cols, schema in `schema.sql`)
   - ✅ Phase 2.2a-ii: `sources/banking_metrics.py` — Screener.in bank-page parser, 158-SID backfill **DONE 2026-05-29 (9.1 min)**. Coverage: **131/132 ex-MICRO (99.2%)** — Banks 41/41 (100%), NBFCs LARGE/MID/SMALL 80/81 (98.8%), NBFCs MICRO 20/36 (excluded anyway). 17 failures all 404'd both standalone+consolidated (delisted/micro-shells). 3,365 rows total. PIPELINE wired (`fetch_banking_metrics`, monthly).
   - ✅ Phase 2.2b: `signals/financial_signal.py` **DONE 2026-05-29**. 141 stocks scored, 105 with financial_signal ≠ NULL, 36 INSUFFICIENT (mostly NBFCs missing both GNPA + NII). Z-scored within (industry, cap_tier); renormalized over present components. Wired to PIPELINE_STEPS as `compute_financial_signal` (daily, non-critical, before screener). **Print-only** — `scoring/screener.py` routing deferred to Phase 2.2d after t-stat ≥ 2.0 backtest validation. Validation spot-checks (top scorers KTKM/ICBK/BMBK/BJFN match reputation; bottom scorers UTK/UNBK/PNBK/SRTR match known weak names). New `financial_signal_scores` table (17 cols). **TODO** (next session): add `financial_signal` to `daily_snapshots_pit` via `_COLUMN_MIGRATIONS` in db.py + BACKTEST_SIGNALS registry entry. Currently blocked by parallel-session uncommitted edits to db.py.
   - 💤 Phase 2.2c: NBFC GNPA fallback — **PROBED + PARKED 2026-05-29**. Gap confirmed data-not-on-source (33/81 NBFCs Screener-empty: REC/IRFC/BAJAJFINSV/JIOFIN). RBI XBRL portal dead; NSE corporate-filings XBRL lacks GNPA% tag (has stage-3 financial assets + ECL but not the ratio); NSE quarterly results PDF is best-fidelity (2-3 day PDF extraction build). Parked because backtest sample size dominates as the rate-limiter — `financial_recovery` MID at t=1.55 crosses 2.0 with ~6 more quarterly periods if mechanism holds.
   - ⚠ Phase 2.2d: PIT + backtest **DONE 2026-05-29**. Composite FAILED done gate (t = -0.75 / -1.30 / -0.34). Diagnostic surfaced direction-flip on NPA — see 2.2b-v2 below.
   - ✅ Phase 2.2b-v2: **DONE 2026-05-29** (commit `0d8d8bd`) — split into `financial_quality` (SMALL, direction='lower') + `financial_recovery` (LARGE/MID, direction='higher'), shared profitability/capital/funding components. Schema migrated; PIT helper writes both columns + back-compat `financial_signal` alias. Backtest on 30 monthly PIT anchors: `financial_recovery` MID **t=+1.55 WEAK** (mechanism confirmed), SMALL **t=-1.88 WEAK** (confirms SMALL quality direction). Neither clears |t|≥2.0; both stay on bench, NOT routed into screener. Re-test ~Q1 FY27. See [proposed ADR 0032](../decisions/0032-tier-direction-flip-split-signal.md) for the generalizable methodology.
   - ✅ Phase 2.2b-v2 follow-up: **FACTOR_LINEAGE entries for the 3 split factors** (`financial_quality` / `financial_recovery` / `financial_signal`) — 2026-05-30. They shipped into `BACKTEST_SIGNALS` without lineage entries, firing the `LINEAGE_REGISTRY_DRIFT` CRITICAL ("lineage before ranking"). Backfilled in [lineage.py](../../lineage.py); health pulse green.

   **Coverage report (latest-quarterly per stock, 2026-05-29):**
   - Banks: 41/41 stocks · GNPA 40/41 · NNPA 40/41 · NII 41/41 · BVPS 41/41 · Deposits 41/41 · COF 41/41 ✓
   - NBFCs: 100/100 stocks · GNPA 20/100 (big gap) · NNPA 19/100 · NII 70/100 · BVPS 95/100 · Borrowings 70/100 · COF 63/100 · Deposits 0/100 (expected — non-deposit-taking)
- ⏳ 2.3 Cyclical overlay (parallel-able with 2.2)
- ⏳ 2.4 Segment models + portfolio (capstone) **↔ 3.3c**
- 🚫 2.5 XGBoost overlay **↔ 3.3b** — needs ≥6mo PIT, ETA early 2027

## Track 3 — Factor model  · [plan 0002](0002-100-factors-and-model.md)
**State**: 47/50 PIT-shipped (+1 industry_id control + 4 macro betas 2026-06-02: all benched, gate PROMOTEs gold/metals at 126d — see §3.2.6/§3.2.7; +4 PEAD/event 2026-05-31: core earnings half did NOT replicate — `earnings_surprise_std`/`pead_drift_60d` DROP/WEAK (no announce dates+consensus); `corporate_action_density` LARGE t=-3.67 KEEP but unclear-mechanism, not promoted; all bench; +6 daily microstructure 2026-05-31: **`kyle_lambda` LARGE t=+4.24 + MID t=+4.14 KEEP** — Amihud illiquidity premium; other 5 DROP; +4 F&O IV: **`iv_skew_25d` MID t=+3.16 KEEP (48 wk)**; +4 F&O OI all bench; +`sector_momentum` bench). **`iv_skew_25d` PROMOTED → live MID screener factor** (weight 0.18, orthogonal); `kyle_lambda` benched (cost-coupled liquidity tilt). **Promotion wave 2026-05-31**: pt_upside (L/M/S, capped), pledge_quality (S), delivery_anomaly_z (S) brought from idle→production after an orthogonality sweep (eps_growth held — ρ=0.63 w/ consensus). Production screener now **LARGE 6 / MID 8 / SMALL 10 factors** (2026-06-03 horizon-gate review dropped LARGE `momentum`, rebalanced LARGE/SMALL — see Track 3.3a top + [signal-weights.md](../reference/signal-weights.md)); MaxReturn/MaxSharpe variants now use 10 factors (pt_upside, eps_growth wired 2026-05-28 + pledge_quality, delivery_anomaly_z wired 2026-05-29 → `tools.optimize_weights --filter-wired` shows **WIRED_KEYS coverage 100/100/100% LARGE/MID/SMALL**); 7 more KEEPs still on bench (interest_coverage, ccc, nwc_to_revenue, goodwill_to_assets MID; roic, fcf_margin SMALL; eps_revision).

- 3.1 Data acquisition:
   - ✅ 3.1a Screener Premium (2,119/2,448 stocks, 681K rows)
   - ✅ 3.1b NSE F&O OI + IV — **shipped 2026-05-31** ([ADR 0034](../decisions/0034-fno-oi-data-model.md) + [0035](../decisions/0035-fno-iv-derived-from-bhav.md)). `sources/fno_pull.py` → `fno_bhav`/`fno_pcr_history`; `sources/fno_iv.py` → `fno_iv_history` (Black-76 IV inversion). **Backfilled to 12mo (250 dates, 2025-05-26→2026-05-29)**, daily PIPELINE_STEPS (`fetch_fno_bhav`/`compute_fno_pcr`/`compute_fno_iv`). Unblocked BOTH halves of §3.2.2.
   - ⏳ 3.1c Kite Connect — **scaffold shipped 2026-05-31, PENDING CREDS** ([sources/kite_pull.py](../../sources/kite_pull.py) + [setup doc](../reference/kite-setup.md)). Auth/token-refresh (TOTP) + instrument map + bar backfill + daily aggregates built; NOT wired to PIPELINE_STEPS until `--check-auth` verifies a live key. Needs: Connect dev-app (₹500/mo) + 5 env exports in `run_pipeline.sh`. Only required for the 3 true-intraday §3.2.3 factors.
   - ⏳ 3.1d PIB + earnings call NLP
- 3.2 Factor build (target 50):
   - ✅ §3.2.1 forensic/capital-allocation — 11/15 shipped; `pt_revision_yoy` DROPPED 2026-05-23 (ADR 0020); rebuild from snapshots calendar 2027-05+
   - ✅ §3.2.2 options-implied (8) — **FULLY SHIPPED 2026-05-31** (8/8, both halves):
      - ✅ OI/grid factors (4, off `fno_pcr_history`): `pcr_oi`, `pcr_volume`, `oi_buildup_signal`, `max_pain_distance` — backtested (22 weekly periods). `pcr_volume` SMALL t=-1.69 + `max_pain_distance` MID t=-1.68 WEAK; `pcr_oi`/`oi_buildup_signal` DROP. All bench.
      - ✅ IV factors (4, off `fno_iv_history` — **derived in-house via Black-76 inversion of fno_bhav settle prices, NOT forward-only as feared; validated vs India VIX**, [ADR 0035](../decisions/0035-fno-iv-derived-from-bhav.md)): `iv_skew_25d` **MID t=+4.61 KEEP** (CI [2.28,9.84] strictly >0 — standout candidate, not yet wired), `iv_realised_spread` MID t=-1.95 WEAK, `iv_term_structure` MID t=-1.80 WEAK (SMALL "KEEP" is a 7-period artifact), `iv_percentile_1y` DROP. fno_bhav backfilled to 12mo (250 dates) so percentile's 1y window fills.
   - ⏳ §3.2.3 microstructure (6/9 shipped 2026-05-31) — [signals/microstructure.py](../../signals/microstructure.py) off daily `stock_prices` OHLCV, backtested on the **deep 39-month panel**. **`kyle_lambda` (Amihud illiquidity) LARGE t=+4.24 + MID t=+4.14 KEEP** (CIs strictly >0) — the illiquidity premium; promotion candidate but cost-coupled + likely colinear with size/adtv → needs `factor_correlation` + cost-aware review. Other 5 DROP (`opening_gap_freq` MID t=1.31, `bidask_spread_proxy` MID t=1.30 weak hints; `closing_strength`/`vwap_deviation`/`range_compression` no signal). **3 remaining ON HOLD** (need Kite intraday → 3.1c): `volume_clock_concentration`, `tick_imbalance_5d`, `intraday_momentum_persistence`.
   - 🚫 §3.2.4 NLP/sentiment (7) — blocked on 3.1d
   - ⏳ §3.2.5 event-time/PEAD (4/6 built 2026-05-31, [signals/pead.py](../../signals/pead.py)) — **core PEAD did NOT replicate.** `earnings_surprise_std` (time-series SUE) DROP all tiers; `pead_drift_60d` SMALL t=-1.54 WEAK (reversal sign). Root cause: no true earnings-announcement dates + consensus (quarterly_income has neither) → SUE/drift proxy too noisy. `corporate_action_density` LARGE t=-3.67 KEEP but unclear mechanism (maturity/value proxy?) — NOT promoted, verify vs value factors first. `buyback_announcement_30d` DROP (too sparse). All 4 bench. **Deferred 2/6**: `dividend_change_signal` (brittle Rs-text parse), `index_inclusion_proximity` (needs historical mcap — current snapshot = look-ahead).
   - ✅ §3.2.6 industry dummies (1) — **shipped 2026-06-02** ([signals/industry_id.py](../../signals/industry_id.py)). One frozen integer-coded categorical CONTROL (38 industries → 1..38, 0=unknown), not rankable alpha → status=CONTROL, kept out of SIGNAL_COLUMN_MAP / IC roster; exists for industry one-hot at model-fit time.
   - ✅ §3.2.7 macro extensions (4) — **shipped 2026-06-02** ([signals/macro_betas.py](../../signals/macro_betas.py)). Built as per-stock rolling 252d **betas** not levels (a level is cross-sectionally constant → 0 IC): `oil_beta`/`metals_beta`/`inr_beta`/`gold_beta`. The named `india_credit_spread`/rate factor is DATA-BLOCKED (no daily India G-Sec; india_money_rate monthly+stale) → `gold_beta` takes the 4th slot. Backtest (23 monthly): `metals_beta` LARGE t=+1.78 + `gold_beta` LARGE t=+1.58 WEAK (CIs straddle 0), oil/inr DROP → all benched (`FACTOR_LIBRARY`). _Note: the promotion gate (ADR 0038) PROMOTEs gold/metals at their 126d natural horizon — n thin, revisit._
- 3.3 Model upgrade (gated on 3.2 ≥ 25 factors; informal advance 2026-05-28):
   - ⏳ 3.3a IC stability — `SIGNAL_WEIGHTS_SHARPE` $w_i ∝ ICIR$ shipped, not yet wired. **Horizon-resolved net-of-cost promotion gate SHIPPED 2026-06-02** ([ADR 0038](../decisions/0038-horizon-resolved-promotion-gate.md), [tools/promotion_gate.py](../../tools/promotion_gate.py) + `factor_horizon_gate`): judges each factor at its cost-resolved natural horizon (net-of-cost annualised IR drives both horizon + verdict). First read PROMOTE 36/LIBRARY 40/REJECT 163. **Acted on the live re-eval 2026-06-03** (commit `a8ecd4b`): `_live_keys()` alias bug fixed (now 24 deployed pairs, 11 clear); deliberate LARGE cleanup (dropped momentum, equalized diversifiers, trimmed EY, lifted consensus/B-P) + SMALL pledge→B-P trim, MID held (conflicts); surfaced on `/system`; `smart_money_score` registered+backtested. See Track 3.3a top.
   - 💤 3.3b Orthogonalization **↔ 2.5**
   - 💤 3.3c Mean-variance portfolio **↔ 2.4**
   - ⏳ 3.3d MaxReturn — `SIGNAL_WEIGHTS_RETURN` $w_i ∝ |t|$ shipped, same promotion gate
   - 💤 3.3e Risk decomposition (Barra-style)

## Side plans
- ✅ [0005 Data confidence 75 → 95](0005-data-confidence-to-95.md) — Phases A–E shipped (~93/100). Phase F (per-stock lineage wave 1) done; wave 2 = roll across remaining ~31 signal modules.
- ✅ MF research section (plans 0001-MF + zazzy-eich) — Phases 1-4 shipped; investable-only default + ETMoney matcher + holdings auto-scrape complete.
- ✅ Ops cockpit split — Stage 1 (service-level :3001) + Stage 2 (code-level extraction).
- ✅ Cockpit cold-restart perf rewrite — `/system` 140×, `/news` 50×, `/portfolio` 100×.
- ✅ Health Center cockpit redesign (ADR 0023).
- ✅ **Offsite DB backup** (2026-05-31) — VM-only `backup_db.sh` → nightly `VACUUM INTO`/integrity/gzip(534MB)/rclone to Google Drive (5 TiB, scope=drive.file), 05:00 UTC cron, 7-daily+monthly retention, restore-verified. First real concrete disaster-recovery path for the 2.6GB SQLite DB.
- ✅ Cockpit mechanical cleanup (2026-05-31, commits `3be4e9a` + cockpit_ops half in `22f1dcf`) — **zero behavior change, verified**. New [cockpit/_shared.py](../../cockpit/_shared.py) (cache decorators + canonical `safe_json_records`, breaks the cockpit_ops→cockpit.api coupling) + [cockpit/mf.py](../../cockpit/mf.py) (9 MF fns lifted out, re-exported from api). `cockpit/api.py` −439 LOC; `get_command_centre` 671→467 LOC split into `_cc_plans_and_adrs` + `_cc_factor_library`; 3 divergent NaN→None coercions deduped. Excluded deliberately (behavior changes, not splits): grade-threshold "dedup" (two genuinely different scales), `macro_gov.py` hardcoded key, and the signal-scoring drift-killers (PIT-gated). Evidence-based critique found NO dead code; db.py/health.py length is principled config-as-code.
- ✅ Factor Health drive-by (2026-05-31): **Promotion Funnel & Orthogonality** section on `/system`. [get_factor_health()](../../cockpit_ops/api.py) summary now carries `funnel` (total/validated/live/waiting/insufficient/dropped/no-backtest — live = wired into `config.SIGNAL_WEIGHTS*`, NOT the conflated `in_model` flag) + `orthogonality` (top |ρ|≥0.8 redundant pairs from the last `tools/factor_correlation` run, flags waiting-dupes-live). Rendered as a tile row + redundancy chips in [cockpit_ops/templates/system.html](../../cockpit_ops/templates/system.html). Today: 66 total · 33 validated · 13 live · 22 waiting · 8 insufficient; 9 redundant pairs, 1 (`consensus`~`eps_revision_yoy`) duplicates a live factor.
- ✅ `/system` cold-load perf (2026-05-31): **21.6s → 14.1s** (warm still instant). Root cause: the page's heavy calls aren't on DuckDB (ADR 0031 only moved model/roster reads) and ran serially. Three low-risk fixes: (1) [db.py](../../db.py) `data_health(cache_ttl=)` lock-guarded memo — freshness scan (~7s) no longer computed twice (`get_data_freshness` + `health_report._gather_tables` share it); no-arg path stays uncached for `freshness_watchdog`'s heal-verify loop. (2) [health_report.gather()](../../tools/health_report.py) runs its 5 sub-gatherers in a threadpool (18.2s → 11.8s). (3) [cockpit_ops/app.py](../../cockpit_ops/app.py) `/system` handler fires its 6 calls via `asyncio.to_thread`/`gather`. **Floor is now ~11s** = `data_sanity.run()` looping 25 SQL checks serially on one connection — next lever is moving those + `data_health` scans to DuckDB (`read_sql_fast`) or caching sanity on a longer TTL.
- ✅ Health drive-by (2026-05-30): `pick_outcomes` STALENESS_OVERRIDE → 14d ([db.py](../../db.py)). `latest_date`=MAX(pick_date) is forward-return-window-bound (shortest 5d window ≈ 7-10 cal days), so the daily(3) default flagged STALE every day + the watchdog heal step FAILED daily trying to "fix" a structural lag. Same class as the filing-cycle overrides.
- ⏳ [0007 Market-share momentum cluster](0003-market-share-momentum-factor.md) — 4 factors, ~7 hr, proposed
- ⏳ [0008 Consumer demand pulse](0004-consumer-demand-pulse.md) — research-gated
- ⏳ **Multibagger identification model** (2026-06-03, user ask off an HBL Engineering "+727%" news item) — a SEPARATE upper-SMALL/MID screen (`multibagger_scores`, kept OUT of `daily_picks`) for 3x–10x/2–4yr candidates. **Design = 3-stage hurdle/filter funnel** (NOT a flat weighted composite), consolidated from 3 research reports in `docs/reference/` (A verified `multibagger-research.md`, B `Quantifying Indian Multibagger Stocks.md`, C `deep-research-report.md`): **S1 hard exclusion gates** (Beneish M>−2.22, CFO/EBITDA 6y, CWIP/GrossBlock, contingent-liab, auditor-fee, pledge>10%, D/E>0.5) → **S2 fundamental hurdles** (mcap ₹1–20k Cr, ROCE>15, PAT-CAGR>15–20, PEG<1, CFO/PAT>0.8, F≥6, promoter>35%) → **S3 composite rank of survivors** anchored on **gross_profitability (Novy-Marx, A's #1 — NEW)** + growth×valuation **interaction** (not additive) + smart-money + catalyst velocity; regime overlay = small-cap EMA trend (C). NEW build-today factors: gross_profitability, beneish_m_score, cfo_to_ebitda_6y, cwip_to_gross_block, peg_ratio, pat_cagr_3y, cfo_to_pat, earnings_acceleration. Data unlock (Amit: will backfill) → **survivorship master** (deaths vs graduations, NO flat −4.5% penalty), SME-migration, order-book/TTM, auditor/contingent, earnings calendar. Validated on **tail-capture** (top-decile ≥3x hit-rate / right-tail capture / decile lift, NOT rank-IC) via NEW `tools/survivorship_panel.py` + `tools/multibagger_cohort.py`. Surfaced on `/multibagger`. Plan: `~/.claude/plans/i-was-going-theough-polished-dolphin.md`. **STATUS 2026-06-03: Phase 0 (deep-research, 3 reports in docs/reference/) DONE. Phase 1 BUILT + VERIFIED — `signals/gross_profitability.py` (Novy-Marx anchor, full contract + PIT, goods-business floor, top=Colgate/Page/Nestlé; 20d backtest DROP n=6 but wrong-lens per ADR 0036) + `signals/multibagger.py` 3-stage funnel + `multibagger_scores` table (1667→955 gates→35 credible survivors: TIPS/Steelcast/Manyavar/Natco). Data landmines caught: market_cap_cr is in RUPEES not crores (÷1e7); stocks.debt_to_equity/pe_ratio empty (computed from Screener); quarterly_income too shallow (growth from ANNUAL Net profit). Phase 1b Beneish gate = REUSED forensic_scores.m_score_flag. NOT yet wired to pipeline/cockpit. Phase 2b DONE — survivorship-corrected + split-adjusted cohort study (`tools/build_historical_universe.py` + `historical_universe` via bhavcopy archive incl. delisted; `tools/multibagger_cohort.py`; `corporate_actions` backfilled to 2018-03). **TWO-REGIME FINDING ([ADR 0039](../decisions/0039-multibagger-funnel-regime-dominated.md)):** 2022→26 junk rally quality top-decile UNDERperforms (spread −0.30x, bottom>top); 2018→21 quality-led bear OUTperforms (+0.10x, top>bottom). ⇒ 2–4yr multibagger capture is REGIME-DOMINATED, not quality-dominated → next = regime gate (small-cap EMA) + regime-conditioned weights, NOT more factors. Plan: [0008-multibagger-model.md](0008-multibagger-model.md). Validation-stage, NOT wired to pipeline/cockpit. NOTE: pre-existing 2026-06-03 cron-freshness drive-by (CLAUDE.md/config.RAW_TABLES/db.STALENESS/nselib_pull.py) intermingled in config.py/db.py — commits together (`git add -p` unavailable).**

## Open questions
- 2.2 banking-metrics source: Tickertape-first or RBI-first?
- 2.3 commodity-data gaps: skip cement/steel until manual curation?
- 0008 paid pytrends fallback if free tier blocks?
- Insider / regulatory / macro signal weights: tertiary 0.2× for first two, zero for macro?
- `pt_upside` |t|=7.20 LARGE after PT cleanup — real alpha or artifact? Re-test after ≥3 monthly snapshots (calendar 2026-08).

## Decisions changing roadmap
- [0009](../decisions/0009-factor-track-parallel-to-d-track.md) — Tracks 2 & 3 parallel; integration points 2.4↔3.3c, 2.5↔3.3b
- [0013](../decisions/0013-industry-not-sector-as-drill-unit.md) — industry replaces GICS sector as drill unit
- [0015](../decisions/0015-track-numbering-and-rename.md) — track naming convention
- [0016](../decisions/0016-plan-numbering-fresh-start.md) — plans renumbered 0001-0004
- [0017](../decisions/0017-factor-library-two-tier-registry.md) — explicit two-tier `BACKTEST_SIGNALS` + `FACTOR_LIBRARY`
- [0018](../decisions/0018-pt-data-model-episodic-cadence.md) → superseded in part by 0020
- [0019](../decisions/0019-observability-sensor-surface-alert.md) — sanity assertions + daily health + push alerts
- [0020](../decisions/0020-pt-data-model-v2-sell-side-only-llm-narrative-only.md) — `forecast_history.price` contaminated; sell-side PT = yfinance only; LLM narrative-only
- [0021](../decisions/0021-pick-eligibility-gate.md) — `daily_picks` requires weight ≥0.50 + price_rows ≥60 + fundamental_coverage ≥0.50
- [0022](../decisions/0022-per-factor-backtest-cadence-newey-west.md) — per-factor cadence + Newey-West; uncovered 3 KEEPs
- [0023](../decisions/0023-health-center-cockpit-as-single-window.md) — `/system` is single window; `get_health_overview()` aggregates 5 sources
- [0024](../decisions/0024-per-signal-eligibility-and-per-stock-integrity.md) — per-signal eligibility registry + per-stock integrity validator
- [0025](../decisions/0025-pit-replay-validator.md) — PIT replay gate on scoring/signals/sources/eligibility pushes
- [0026](../decisions/0026-micro-tier-carve-out.md) — MICRO 4th cap-tier carved out of SMALL
- [0027](../decisions/0027-per-stock-data-lineage.md) — `FACTOR_LINEAGE` + `TABLE_COLUMN_SOURCES` + `signal_lineage` table
- [0028](../decisions/0028-two-variant-factor-model.md) — RETURN + SHARPE weight variants, promotion deferred
- [0029](../decisions/0029-mf-investable-only-default.md) — MF universe defaults to investable cut
- [0030](../decisions/0030-banking-metrics-screener-first.md) — banking_metrics source = Screener.in (not Tickertape); 158-SID scope
- [0031](../decisions/0031-duckdb-read-replica.md) — DuckDB read-replica rebuilt nightly; `read_sql_fast` routes mirrored-table reads; SQLite remains write-side source of truth
- [0032](../decisions/0032-tier-direction-flip-split-signal.md) — when a factor's IC flips sign across cap_tiers, split into two named signals (`X_quality` / `X_recovery`); don't apply per-tier sign on the composite
- [0034](../decisions/0034-fno-oi-data-model.md) — F&O OI data model: `fno_bhav_copy` EOD grid is the backfillable workhorse (one call = whole market, no per-symbol loop); store full grid filtered to oi>0∨vol>0; OI/PCR/max-pain factors are backfill-fed (not clock-gated). _[IV "forward-only" claim here SUPERSEDED by 0035.]_
- [0035](../decisions/0035-fno-iv-derived-from-bhav.md) — F&O IV/Greeks derived **in-house** by Black-76 inversion of `fno_bhav` settle prices (no paid feed, fully historical); validated vs India VIX. Supersedes 0034's "IV is forward-only". `iv_term_structure` is thin at single-stock level (~20%) — index-level signal.
- [0037](../decisions/0037-per-stock-uhs-and-pt-plausibility.md) — UHS plausibility/consistency dims are now **per-stock** (sid-filtered `_gate_pass_rate`, fallback to universe mean) so the badge reflects the individual stock, not a tier-wide average; + layered PT-plausibility guards (per-fetch gate / stored sweep / display / data_sanity) so Yahoo garbage targets can't reach the display or read TRUSTED. Extends [ADR 0033](0033-trust-pipeline-uhs.md). _Known-gap (raw-vs-derived `FACTOR_UPSTREAM_TABLES` mismatch) RESOLVED 2026-06-02, commit `2978d04` — repointed at `piotroski_scores`._
- [0036](../decisions/0036-horizon-resolved-factor-evaluation.md) — factors have heterogeneous natural horizons; single-20d t-stat conflates wrong-horizon with no-alpha. Shipped read-only IC term-structure diagnostic (`tools/ic_decay.py`, grid 5/20/63/126/252td, FAST/MED/SLOW). Proposed: natural_horizon registry + net-of-cost-IR promotion gate (↔ 3.3a). Also: pick_outcomes → trading-day windows; benchmark fetch wired.
- [0038](../decisions/0038-horizon-resolved-promotion-gate.md) — codifies 0036's proposed gate. `tools/promotion_gate.py` + `factor_horizon_gate` emit PROMOTE/LIBRARY/REJECT per (signal,tier) at the **cost-resolved natural horizon**; net-of-cost annualised IR drives both horizon choice + verdict (so we never classify off raw peak |IC|). Cost in IC units = T·c_side/σ(fwd_h) → shrinks with horizon → fast factors charged more (turnover-aware). Evidence surface only; weights stay a human decision. Default turnover 0.3. ↔ 3.3a.
- [0039](../decisions/0039-multibagger-funnel-regime-dominated.md) — multibagger screen = SEPARATE 3-stage hurdle/filter funnel (`multibagger_scores`, OUT of `daily_picks`), Novy-Marx `gross_profitability` anchor, validated by survivorship-corrected cohort (not rank-IC). **Key finding: 2–4yr multibagger capture is REGIME-DOMINATED** — quality wins bears, loses junk rallies → regime gate essential; never calibrate to one window. ↔ plan 0008.
- [0040](../decisions/0040-multibagger-holding-not-selection.md) — multibagger is a HOLDING problem, not selection: no at-entry factor ranks 2–4yr winners (stock-score ρ−0.06), SECTOR dominates (ρ+0.41) but is mechanically unpredictable; sector momentum works only 1–6mo (t+3) → the MONITORING signal. Product = junk-stripped pond → 20–30-name equal-weight basket → HOLD (no stops; they eject 81% of winners) → rolling HOLD/WATCH/REVIEW conviction monitor. Extends 0039. ↔ plan 0008 Phase 5.

## Recently archived
- 0001 regulatory signal · 0002 macro data · 0004 PIT reconstruction · 0006 sector intelligence
